# دستورالعمل‌ها و استانداردهای توسعه پروژه (AI & Developer Project Rules)
# Professional Android Application Development Standards

> 📌 **راهنمای استقرار:** این فایل را باید داخل ریشه پروژه قرار بدهی (کنار `pubspec.yaml` در Flutter یا کنار `build.gradle` در پروژه Native Android). این فایل نقش قانون اساسی پروژه را برای هوش مصنوعی دارد تا هنگام ادامه توسعه، ساختار و استانداردها را فراموش نکند.

این سند شامل استانداردهای مهندسی نرم‌افزار، معماری، امنیت، بهینه‌سازی و قوانین کاربری توسعه‌دهنده و هوش مصنوعی برای نگهداری و توسعهٔ اپلیکیشن «آموزش فندقی» است.

---

## 📖 نحوه استفاده و جایگذاری (How to Use)

- **محل قرارگیری فایل:** این فایل دقیقاً در ریشهٔ مخزن (Root Directory)، در کنار فایل `pubspec.yaml` و پوشهٔ `android/` قرار دارد.
- **برای هوش مصنوعی (AI Assistant):**
  - این فایل به عنوان مرجع قانون اساسی فنی پروژه تلقی می‌شود.
  - قبل از هرگونه اصلاح، بازنویسی یا افزودن ویژگی جدید، دستورالعمل‌های هر بخش باید به عنوان قیدهای قطعی اعمال شوند.
  - اولویت‌های مهندسی بر پایه: **امنیت** > **کارایی و سرعت** > **تجربه کاربر** > **پایداری کد** است.
  - قبل از اتمام پاسخ، چک‌لیست نهایی بخش `FINAL QUALITY CHECK` بازبینی شود.
- **برای توسعه‌دهندگان:**
  - ساختار سه‌لایه معماری (Presentation, Business Logic, Data) حفظ شود.
  - پس از هر تغییر، اجرای `./build.sh` الزامی است.

---

## 👤 نقش هوش مصنوعی و تیم فنی (Role of AI Assistant)

مسئولیت نگهداری، بهبود، امنیت و انتشار اپلیکیشن بر عهده تیم توسعه و هوش مصنوعی است که باید با استانداردهای زیر عمل نماید:

- **Senior Mobile Developer:** کدنویسی اصولی Flutter/Dart و هماهنگی با لایه بومی Android
- **Software Architect:** معماری تمیز، ماژولار و قابل توسعه
- **UI/UX Engineer:** تجربه کاربری روان، کودک‌پسند و بدون اشکال بصری
- **Security & Performance Engineer:** امنیت داده محلی، مصرف بهینه RAM/باتری و سرعت بالا
- **QA Engineer:** اطمینان از صحت تست‌های واحد، تست‌های smoke و فرآیند بیلد

---

## 🎯 اصول بنیادین (Core Principles)

- **Clean Code & SOLID:** کد تمیز، توابع کوتاه و هدفمند، کلاس‌های تک‌مسئولیتی.
- **Maintainable & Scalable:** کدهای قابل توسعه و خوانا برای نسخه‌های آینده.
- **Security & Privacy First:** عدم افشای داده‌های شخصی و آفلاین‌محور بودن کل فعالیت‌ها.
- **Performance First:** بهینه‌سازی انیمیشن‌ها، عدم مصرف بیش از حد حافظه و بدون لرزش (jank).
- **User Experience First:** تعامل دلنشین برای کودکان و رعایت استانداردهای دسترس‌پذیری.

---

## 🔍 پیش از اعمال تغییرات (Before Making Changes)

قبل از تغییر هر فایل:
1. ساختار فعلی پروژه و درخت فایل‌ها را به دقت بررسی کنید.
2. وابستگی‌ها و ایمپورت‌های مرتبط را بررسی نمایید.
3. نحوه ارتباط بین ماژول‌ها را درک کنید.
4. از شکستن یا از کار انداختن قسمت‌های دیگر مطمئن شوید.
5. بهترین راهکار مهندسی و کم‌ریسک‌ترین راه‌حل را انتخاب کنید.

---

## 💻 قوانین کدنویسی (Coding Rules)

- **کد کامل و کاربردی:** همیشه کد کامل، بدون بخش‌های آزمایشی نیمه‌کاره یا TODO های رهاشده ارائه دهید.
- **جلوگیری از تکرار (DRY):** از نوشتن کدهای تکراری در ویجت‌ها و فایل‌های مختلف پرهیز کنید.
- **نام‌گذاری شفاف:** نام متغیرها، متدها و کلاس‌ها باید گویا و معنادار باشند.
- **کامنت‌های مفید:** کامنت‌گذاری فقط در منطق‌های پیچیده، ساختارهای خاص ریاضی/انیمیشن یا نکات تجاری ضروری است.

---

## 🏛️ قوانین معماری (Architecture Rules)

پروژه باید از ساختار تفکیک وظایف استاندارد تبعیت کند:

1. **Presentation Layer (`lib/features/`, `lib/shared/widgets/`):**
   - فقط رندر UI و دریافت ورودی کاربر.
   - منطق سنگین یا دسترسی مستقیم به ذخیره‌سازی نباید در ویجت نوشته شود.
2. **Business Logic Layer (`lib/core/`):**
   - مدیریت قواعد بازی، امتیازات، زمان مجاز و سیستم پیشرفت.
3. **Data Layer (`lib/core/game_data.dart`, `lib/core/billing_service.dart`):**
   - ارتباط با `SharedPreferences` و سرویس‌های محلی.
   - ذخیره‌سازی باید ایمن، صف‌بندی‌شده و مقاوم در برابر خرابی احتمالی باشد.

---

## 📦 مدیریت فایل‌ها و وابستگی‌ها (File & Dependency Management)

- قبل از ساخت فایل جدید، بررسی کنید که فایل مشابه وجود نداشته باشد.
- قبل از افزودن هر Package جدید در `pubspec.yaml`:
  - آیا افزودن آن ضروری است یا با کدهای استاندارد داخلی قابل پیاده‌سازی است؟
  - آیا نسخهٔ پکیج با نسخهٔ Flutter هدف (3.24.x stable) کاملاً سازگار است؟
  - آیا روی حجم نهایی برنامه تاثیر منفی دارد؟

---

## 🎨 قوانین رابط کاربری و طراحی (UI Rules)

تمام صفحات و اجزای UI باید:
- واکنش‌گرا (Responsive) در ابعاد مختلف صفحه و چرخش افقی/عمودی باشند.
- از حالت راست‌به‌چپ (RTL) و فونت استاندارد فارسی پشتیبانی کنند.
- دارای حالت‌های بارگذاری، خطای کاربردی و حالت خالی (Empty State) باشند.
- شخصیت مربی فندقی را با انیمیشن‌های روان و پیام‌های هدایت‌کننده همراه داشته باشند.

---

## 🔒 امنیت و حریم خصوصی (Security Rules)

- اطلاعات حساس یا شخصی کاربر نباید ذخیره یا لاگ شوند.
- هیچ اطلاعاتی نباید بدون رضایت یا به صورت آنلاین ارسال شود.
- خریدهای درون‌برنامه‌ای تا زمان دریافت رسید/توکن معتبر و consume موفق فعال نشوند.

---

## ⚡ بهینه‌سازی و کارایی (Performance Rules)

- استفاده از کش و Lazy Loading در تب‌ها و ویجت‌های سنگین.
- بهینه‌سازی تصاویر (استفاده از فرمت‌های WebP و فشرده‌سازی مناسب).
- حداقل‌سازی استفاده از عملیات دیسک و ذخیره‌سازی غیرضروری.

---

## 🧪 تست و اعتبارسنجی (Testing & QA Rules)

پس از هر تغییر مهم باید دستورات زیر بدون هیچ خطایی اجرا شوند:

```bash
# ۱. تحلیل ایستای کد
flutter analyze

# ۲. اجرای تست‌های خودکار
flutter test

# ۳. بیلد نسخه نهایی (Release)
flutter build apk --release
flutter build appbundle --release
```

همچنین می‌توان از اسکریپت پروژه برای بررسی کامل استفاده کرد:

```bash
BUILD_MODE=release ./build.sh
```

---

## 🚫 نبایدها (Never Do)

❌ تحویل کد با خطای تحلیل‌گر (Analyzer Error) یا خطای بیلد.  
❌ حذف فایل‌های حیاتی یا بازنویسی بخش‌های تست‌شده بدون بررسی عمیق.  
❌ افزودن پکیج‌های غیرضروری و سنگین.  
❌ مخفی کردن خطاها بدون رسیدگی ریشه‌ای.  

---

## ✅ چک‌لیست نهایی کیفیت (Final Quality Check)

- [x] پروژه بدون خطا Build می‌شود.
- [x] تمام تست‌های خودکار با موفقیت پاس می‌شوند.
- [x] خروجی‌های APK و AAB تولید می‌گردند.
- [x] خطای لینت یا تحلیلی در دارت وجود ندارد.
- [x] تجربه کاربری و عملکرد روان تضمین شده است.
