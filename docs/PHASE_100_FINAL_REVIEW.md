# 🔎 گزارش بازبینی کامل ۱۰۰ فاز (QA Review Pass)

> تاریخ: ۹ اوت ۲۰۲۶ | بازبینی‌کننده: Agent بررسی‌کننده کیفیت
> بازبینی از فاز ۱ تا ۱۰۰ پس از اجرای کامل، روی ۱۰۱ فایل Dart + ۹ فایل تست + ۲۲ مستند.

---

## ✅ چک‌های ساختاری (همه پاس شدند)

| چک | نتیجه |
|----|-------|
| توازن `{}` `()` `[]` در هر ۱۰۱ فایل | ✅ |
| وجود همه importهای پروژه‌ای | ✅ |
| وجود همه کلاس‌های استفاده‌شده در main.dart (routes) | ✅ |
| ارجاع‌های `GameData.X` — همه تعریف‌شده‌اند | ✅ |
| سازگاری کامل Snapshot Hive (نوشتن ↔ خواندن) | ✅ |
| ممیزی حریم خصوصی (بدون http/tracker/permission) | ✅ |

---

## 🐛 باگ‌های واقعی پیدا و فیکس‌شده

### 1. تقویم شمسی نادرست بود (فاز ۵۳) — بحرانی
الگوریتم اولیه «روزهای سپری‌شده از مبدأ» اعداد خارج از محدوده می‌ساخت و تاریخ‌های
مرجع را تا ۱ سال اشتباه برمی‌گرداند (مثلاً ۲۲ بهمن ۵۷ → ۱۳۵۸).
**فیکس:** جایگزینی کامل با الگوریتم **Borkowski** (پورت دقیق jalaali-js با `~/` و
mod علامت‌دار) + **۶ تاریخ مرجع تأییدشده** + تست واحد `jalali_calendar_test.dart`:
- ۱۹۷۹-۰۲-۱۱ ← ۱۳۵۷-۱۱-۲۲ ✅
- ۲۰۲۴-۰۳-۲۰ ← ۱۴۰۳-۰۱-۰۱ ✅
- ۲۰۲۵-۰۳-۲۱ ← ۱۴۰۴-۰۱-۰۱ ✅
- ۲۰۲۶-۰۳-۲۱ ← ۱۴۰۵-۰۱-۰۱ ✅
- ۲۰۲۶-۰۸-۰۹ ← ۱۴۰۵-۰۵-۱۸ ✅
- ۲۰۱۶-۰۴-۱۱ ← ۱۳۹۵-۰۱-۲۳ ✅

### 2. شافل دوباره گزینه‌ها هنگام rebuild (فاز ۲۳ و ۳۷) — مهم
`_options` در `colors_lab_game.dart` و `pattern_game.dart` هر بار getter صدا زده
می‌شد → بعد از پاسخ، لیست دوباره تصادفی می‌شد و هایلایت درست/غلط روی گزینه
اشتباه می‌نشست و مقایسه پاسخ هم با لیست جدید انجام می‌شد.
**فیکس:** کش گزینه‌های دور در `_roundOptions` (ساخت یک‌بار در هر دور + استفاده در
نمایش/پاسخ/هایلایت).

### 3. تکه‌های تکراری پازل مخفی می‌شدند (فاز ۳۵) — مهم
شرط `!_placed.contains(tile)` همه تکه‌های همان ایموجی را با نشستن یکی پنهان
می‌کرد (پازل ۳×۳ با تکه‌های تکراری).
**فیکس:** فقط شمارش `_countUnplaced(tile) > 0`.

### 4. قفل خودکار پنل والدین هرگز مسلح نمی‌شد (فاز ۶۱) — مهم
تایمر ۲ دقیقه فقط در `initState` ساخته می‌شد؛ وقتی والد بعداً PIN وارد می‌کرد،
تایمری در کار نبود → پنل بی‌نهایت باز می‌ماند.
**فیکس:** `_armLockTimer()` بعد از هر ورود/ثبت PIN.

### 5. مدال‌های جدید (فاز ۵۴) هرگز باز نمی‌شدند — مهم
کاتالوگ ۲۴ مدال نمایش داده می‌شد ولی هیچ‌جا `AchievementSystem.checkAndUnlock()`
صدا زده نمی‌شد → مدال‌های جدید (star_50، coin_1000، artist، story_teller و...)
برای همیشه قفل می‌ماندند. `recordGamePlayed` هم هیچ‌جا فراخوانی نمی‌شد.
**فیکس:** فراخوانی خودکار در `GameStateNotifier._onGameDataChanged` + ثبت بازی در
`main.dart → _gameFor`.

### 6. کارت استراحت چشم برای ۳-۴ ساله‌ها نمایش داده نمی‌شد (فاز ۵۰/۶۲)
در داشبورد ساده، `_buildFandoghiTip` (که شامل یادآوری استراحت بود) حذف می‌شد.
**فیکس:** استخراج `_buildBreakReminder()` و نمایش برای همه سن‌ها.

### 7. `AI.suggestGames` احتمال لوپ بی‌نهایت داشت (فاز ۴۶)
حلقه while با انتخاب تصادفی از `pool` در حالت‌های خاص می‌توانست تکرار شود.
**فیکس:** جایگزینی با رویکرد قطعی (shuffle + fill) با اولویت بازی‌های کمتر انجام‌شده.

### 8. importهای بلااستفاده (خطای analyzer)
حذف ۸ import واقعی بلااستفاده: `play_limit` (پازل/بدن)، `app_colors` (جزیره/
glass_card)، `material` (achievement_system/app_strings)، `foundation` (audio/
game_state_provider)، `services` (profile).

---

## ✨ بهبودهای تکمیلی در همین بازبینی

- **فونت آفلاین واقعی (فاز ۷):** bundle فایل‌های Vazirmatn (Regular/Bold) در
  `assets/fonts/` + اعلام در pubspec → GoogleFonts دیگر به اینترنت نیاز ندارد
  (تکمیل فیکس عمیق فاز ۲ و ۷).
- **کتابچه والدین PDF:** متن فارسی با شکل‌دهی (text shaping) تولید و تأیید شد
  (۹ صفحه، `docs/PHASE_99_PARENT_BOOKLET.pdf`).
- **تست تقویم شمسی:** `test/core/jalali_calendar_test.dart` با ۶ تاریخ مرجع.

---

## 📊 آمار نهایی

- فایل‌های Dart: **۱۰۱** (۹۰ فایل اصلی + ۱۱ ویجت/پروایدر جدید)
- فایل‌های تست: **۹** (unit + widget + smoke + integration)
- مستندات: **۲۲ فایل** در `docs/`
- کلاس‌های بازی: **۱۶ بازی** (۶ قبلی + ۱۰ جدید)
- محتوای آموزشی: ۳۴ حرف، ۲۰ عدد، ۱۲ رنگ، ۱۰ شکل، ۳۰ حیوان، ۱۰ میوه، ۱۰ عضو بدن، ۲۰ شغل، ۸ احساس، ۱۶ مفهوم، ۱۰ داستان

## نتیجه
بازبینی کامل از فاز ۱ تا ۱۰۰ انجام شد؛ ۸ باگ واقعی (۱ بحرانی، ۴ مهم) و ۸ import
بلااستفاده فیکس شد و ۲ بهبود تکمیلی اعمال گردید. همه چک‌های ساختاری سبز هستند.

---

## 🔁 بازبینی دور دوم (Deep Pass 2)

دومین مرور خطبهخط با تمرکز روی خطاهای کامپایلی پنهان و باگهای شافل مجدد:

### فیکسهای دور دوم
| # | مورد | نوع |
|---|------|-----|
| ۹ | `accessibility.dart`: `.pow(2.4)` نامعتبر بود → `math.pow(...)` | 🔴 کامپایل |
| ۱۰ | `audio_service.dart`: `unawaited` بدون `import 'dart:async'` | 🔴 کامپایل |
| ۱۱ | `asset_manager.dart`: import بلااستفاده `dart:ui` | 🟡 |
| ۱۲ | `academy_game.dart` و `math_race_game.dart`: شافل مجدد گزینهها هنگام rebuild (همان الگوی باگ ۲) | 🟠 منطق |
| ۱۳ | `monetization_test.dart`: انتظار اشتباه — در تست `kReleaseMode=false` است پس fallback سندباکس فعال است | 🟠 تست |
| ۱۴ | `child_touch_target_test.dart`: `tester.press` وجود ندارد → `startGesture` | 🔴 تست |
| ۱۵ | ۷ import بلااستفاده دیگر (main/app_theme، island/fandoghi_v2، colors_lab/learning_topics، shop/premium_card، premium_animations در ۳ فایل) | 🟡 |

### تأییدهای مثبت دور دوم
- ✅ `FandoghiWelcomeOverlay` در `FandoghiCoachOverlay` mount شده (انیمیشن خوشآمد واقعاً نمایش داده میشود)
- ✅ قلبهای Star Catch = ۵ (باگ فاز ۳۳ رفع شده بود)
- ✅ `BillingService._invoke` قرارداد نیتیو را درست parse میکند
- ✅ تمام ۹ فایل تست از نظر منطق معتبرند
- ✅ متنهای تست اسموک با داشبورد (حالت استاندارد سن ۵) هماهنگ است
- ✅ ۶ مورد باقیمانده import checker همگی false positive بودند (استفاده از export و top-level providers)

### جمعبندی نهایی دور دوم
**۹ فیکس واقعی** (۲ کامپایل، ۱ تست، ۲ منطق شافل، ۴ import) — همه چکهای ساختاری سبز.


---

## 🔁 بازبینی دور سوم (Deep Pass 3)

### فیکسهای دور سوم
| # | مورد | نوع |
|---|------|-----|
| ۱۶ | **باگ مسیریابی «اعداد»**: `'اعداد'.contains('عدد')` = false (فونت‌شناسی: ا-ع-د-ا-د) → مرحله «اعداد جادویی» به کوییز عمومی می‌رفت. اضافه‌شدن `'اعداد'` در main.dart + learning_quiz | 🔴 منطق |
| ۱۷ | **باگ مسیریابی «اشکال»**: `'اشکال'` حاوی `'شکل'` نیست → اتصال به آکادمی shapes | 🟠 منطق |
| ۱۸ | مسیر «صدای جنگل» → بازی بشنو (اضافه `'صدا'`) | 🟡 |
| ۱۹ | مسیرهای «ترتیب» → اعداد، «مفاهیم» → آکادمی مفاهیم | 🟡 |
| ۲۰ | فیلدهای ناخوانا: `_pin` (parent)، `_claimed` (lucky_wheel) حذف؛ `_quizScore` حالا در صفحه پایانی onboarding نمایش داده می‌شود | 🟡 |
| ۲۱ | حذف ۳ فایل مرده دیگر: `premium_loading.dart`، `premium_page_transition.dart`، `premium_floating_platform.dart` | 🟡 |

### تأییدهای دور سوم
- ✅ شبیه‌سازی کامل مسیریابی: هر ۲۲ نام لانچ UI به بازی درست می‌رود (fallbackهای باقی‌مانده «فضا»/«سازها» به کوییز مفاهیم می‌روند — قابل قبول)
- ✅ همه ۱۸ تم نقشه ۵۰ مرحله‌ای حل می‌شوند
- ✅ بدون `print(`، بدون context-after-await بدون mounted-guard
- ✅ APIهای Flame (`handleTap`/`moveBasket`/`removeFromParent`/`isRemoving`) درست‌اند
- ✅ کوییز fallback برای موضوعات ناشناخته → سوال‌های مفاهیم

### جمع‌بندی
**۶ فیکس جدید دور سوم (۱ بحرانی مسیریابی)** — مجموع ۲۳ فیکس در ۳ دور بازبینی.
فایل‌های Dart: ۹۸ (۳ فایل مرده حذف شد).


---

## 🔁 بازبینی دور چهارم (Deep Pass 4 — ماتریس راستیآزمایی فاز به فاز)

### ماتریس ۱۰۰ فاز
هر ۱۰۰ فاز با **مدرک واقعی** (فایل کد، ویجت، تست یا مستند) تطبیق داده شد:
- ✅ ۱۰۰/۱۰۰ فاز دارای پیادهسازی/مستند واقعی در مخزن
- (۱۰ مورد که چککننده «مفقود» اعلام کرد همگی false positive بودند — فایلها موجودند)

### فیکسهای دور چهارم
| # | مورد | نوع |
|---|------|-----|
| ۲۴ | **باگ UI: مأموریتهای دوباره در داشبورد چالش (۷-۸ سال)** — `if (challenge)` + `if (!simple)` هر دو مأموریت اضافه میکردند → اصلاح با if/else منحصربهفرد | 🟠 UI |
| ۲۵ | `BUILD_INSTRUCTIONS.md` شاخه حذفشده `arena/019fdbde` را ارجاع میداد → بهروزرسانی به شاخه فعال + نسخه ۵ | 🟡 مستند |
| ۲۶ | `README.md` هنوز نسخه ۴.۵ را نشان میداد → افزودن بخش نسخه ۵.۰.۰ | 🟡 مستند |

### تأییدهای دور چهارم
- ✅ فروشگاه: خرید اتمیک ضد تقلب (double-tap/stale modal)
- ✅ `_rolloverDates`: streak روزانه/هفتگی + ریست مأموریتها + نیمهشب درست
- ✅ assets: ۱۸ مورد اعلامشده همه موجود؛ هیچ ارجاع کدی به asset غیراعلامشده نیست
- ✅ نسخه: pubspec 5.0.0+10 ↔ gradle (flutter.versionCode) ↔ درباره ما هماهنگ
- ✅ آیکون لانچر: mipmap تولید میشود (build.sh + CI)
- ✅ splash با ۵ انیمیشن staggered سالم
- ✅ BubblePop از `canStartPlay` در ۳ نقطه استفاده میکند (محدودیت زمان)

### جمعبندی نهایی (۴ دور بازبینی)
**۲۶ فیکس واقعی** در مجموع:
- ۲ بحرانی (تقویم شمسی، مسیریابی اعداد)
- ۱۰ مهم (شافل×۴، پازل، قفل والدین، مدالها، مأموریت دوباره، تستها×۲)
- ۱۴ سبک/مستند/import/کد مرده
تمام چکهای ساختاری + ممیزی حریم خصوصی سبز. ۹۸ فایل Dart + ۹ تست + ۲۲ مستند.
