# ✅ چک‌لیست انتشار — جزیره فندقی (کافه‌بازار + مایکت)

> **نسخه:** ۶.۲.۱+۱ · **package:** `com.parsaapps.amoozesh_fandoghi` · **شناسه محصول پرداخت:** `full_version` (خرید دائمی، غیرمصرفی)
> تیک‌ها را موقع انجام پر کنید. موارد قرمز «بلاک‌کننده» هستند؛ بدون آن‌ها انتشار رد می‌شود یا پرداخت/نصب می‌شکند.

---

## 🔴 بلاک‌کننده — باید انجام شود (در پنل فروشگاه‌ها)

- [ ] **محصول `full_version` در کافه‌بازار ساخته شود** (پنل → پرداخت درون‌برنامه‌ای، نوع «غیرمصرفی/دائمی»، هم‌نام با شناسه کد)
- [ ] **محصول `full_version` در مایکت ساخته شود** (پنل توسعه‌دهنده مایکت، نوع دائمی)
- [ ] **کلید RSA بازار متعلق به همین حساب باشد** — مقدار `bazaarRsaPublicKey` در `android/billing.properties` باید دقیقاً کلید صادرشده از پنلِ خودت باشد (در غیر این صورت امضای رسید رد می‌شود)
- [ ] **کلید RSA مایکت متعلق به همین حساب باشد** — `myketRsaPublicKey` از پنل خودت (در غیر این صورت پرداخت مایکت فعال نمی‌شود)
- [ ] **لینک سیاست حریم خصوصی هاست شود** — متن `PRIVACY_POLICY_FA.md` روی یک صفحه (GitHub Pages / سایت) قرار گیرد و URL در هر دو پنل وارد شود
- [ ] **تست سندباکس کافه‌بازار** — خرید، لغو، قطع اینترنت وسط پرداخت، بازیابی (restore) با حساب تستی
- [ ] **تست سندباکس مایکت** — همان سناریوها با حساب تستی مایکت
- [ ] **امضای release با کلید واقعی** — در CI سبز است (استپ `Verify release signing`)، فقط مطمئن شو keystoreِ در Secrets همان کلید نهایی و بکاپ‌گرفته‌شده است

## 🟡 پر کردن فرم‌های فروشگاه (برای هر دو)

- [ ] **عنوان:** «جزیره فندقی» (یا «جزیره فندقی — آموزش الفبا و بازی کودک»)
- [ ] **توضیح کوتاه + کامل:** از `store/bazaar/STORE_LISTING.md` و `store/myket/STORE_LISTING.md`
- [ ] **دسته‌بندی:** کودک / آموزشی
- [ ] **رده‌بندی سنی:** فرم رده‌بندی محتوا (خردسال) پر شود
- [ ] **آیکون:** `store/bazaar/icon_512.png`
- [ ] **اسکرین‌شات‌ها:** پوشه `store/bazaar/upload/` (۱۰۸۰×۱۹۲۰)
- [ ] **سرصفحه (فقط بازار):** `store/bazaar/header_5x2.png`
- [ ] **کلیدواژه‌ها:** آموزش الفبا، بازی کودک، پیش‌دبستانی، آفلاین، بدون تبلیغ، فارسی، مهدکودک، مهارت زندگی
- [ ] **تماس/پشتیبانی:** تلگرام `@Parsaappsadmin` · ایمیل `farshadparsa2019@gmail.com`

## 🟢 تأیید نهایی پیش از آپلود

- [ ] نسخه در `pubspec.yaml` نهایی شده (اکنون `6.2.1+1`)
- [ ] `flutter analyze` و `flutter test` لوکال سبز (اسکریپت `tool/ci_quality_gate.sh`)
- [ ] آخرین APK/AAB از آرتیفکت CI دانلود شده (رن موفق `Build APK & AAB`)
- [ ] روی یک گوشی واقعی نصب و تست دستی: باز کردن کارتون، خرید آزمایشی، پنل والدین، حالت آفلاین

---

> **یادآوری:** APK و AAB واحد ساخته می‌شوند و هر دو فروشگاه را پوشش می‌دهند (تشخیص خودکار فروشگاه نصب‌کننده). قیمت پیشنهادی نسخه کامل: ۴۹٬۰۰۰ تومان (در پنل هر فروشگاه جدا تنظیم می‌شود).
