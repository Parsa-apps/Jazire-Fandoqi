# 📊 گزارش نهایی نسخه ۷.۰.۰ — «جزیره زنده»

> CTO Office — ۳۰ شهریور ۱۴۰۵ / 2026-09-21
> شاخه: `arena/01a0c3ee-jazire-fandoqi` | Pull Request: **#151**
> ممیزی کامل پیش از پیاده‌سازی: [`AUDIT_V7.md`](AUDIT_V7.md)

---

## نسخه‌ها

| | نسخه | versionCode |
|---|---|---|
| فعلی (مایکت) | 6.2.6 | 6 |
| **جدید** | **7.0.0** | **20** |

ارتقای نسخهٔ اصلی (minor→major) به‌دلیل افزودن دنیای بازی جدید + سیستم پیشرفت جدید — دقیقاً مطابق درخواست «آپدیت بزرگ و چشمگیر».

---

## ✅ قابلیت‌های اضافه‌شده (کامل)

### ۱. شهر بازی‌های تازه 🎪 — ۱۰ بازی آموزشی جدید
| # | بازی | مهارت آموزشی | مأموریت | دسترسی |
|---|------|--------------|---------|--------|
| ۱ | 🔤 حرف اول | آواشناسی — شروع خواندن (اتصال حرف به تصویر) | words | رایگان |
| ۲ | 🧱 کلمه‌ساز | ساخت کلمه با حرف گم‌شده | words | نسخهٔ کامل |
| ۳ | 🔢 شمارش خوش | شمارش ۱–۹ با اشیای آشنا | counting | رایگان |
| ۴ | ➕ جمع تصویری | مفهوم جمع قبل از نماد ریاضی | math | نسخهٔ کامل |
| ۵ | 🐘 بزرگ و کوچک | مقایسهٔ اندازه (دو جهت) | — | رایگان |
| ۶ | 🔷 جورچین شکل‌ها | تشخیص شکل مستقل از رنگ | — | نسخهٔ کامل |
| ۷ | 🎨 ترکیب رنگ | شناخت + ترکیب‌های واقعی رنگ | colors | نسخهٔ کامل |
| ۸ | 🪜 ترتیب اعداد | مرتب‌سازی صعودی (دورهای زنجیره‌ای) | counting | رایگان |
| ۹ | 🕵️ متفاوت را پیدا کن | طبقه‌بندی ذهنی | logic | رایگان |
| ۱۰ | 🧺 سبد دسته‌بندی | دسته‌بندی چندانتخابی با تأیید | logic | نسخهٔ کامل |

**ویژگی‌های مشترک (چارچوب `MiniGameScaffold`):**
- «تلاش دوباره» به‌جای شکست — دور فقط با پاسخ درست جلو می‌رود؛ امتیاز نرم کاهش می‌یابد (۱۰/۵/۲)
- Hint ظریف: بعد از ~۱۰ ثانیه بی‌حرکتی، فندقی راهنمایی می‌کند و پاسخ درست نرم می‌تپد؛ بعد از ۳ اشتباه راهنمای بصری
- لحن مثبت: «تقریباً! دوباره امتحان کنیم 😊» — هرگز «اشتباه است»
- سختی تطبیقی از موتور AI موجود (`AI.difficulty()`)
- ثبت خودکار: XP + مهارت + مأموریت + آمار بازی + مدال
- موسیقی زمینه و ساعت خواب/فیلتر والدین از مسیر `GameAccessGate` (مثل همهٔ بازی‌های قبلی)

### ۲. سیستم پیشرفت کودک — XP و «سطح جزیره» ⭐
- `XpSystem` خالص و تست‌پذیر: پاسخ درست +۲ | هر تلاش +۱ (تنبیه نمی‌شود) | مرحله +۱۰ | پایان بازی +۵
- منحنی ملایم `۲۵·n·(n−۱)`؛ XP هرگز کم نمی‌شود (بدون استرس، بدون قمار)
- ۹ لقب کودکانه: جوجه فندقی → فندقی کوچولو → … → افسانه جزیره
- نمایشگر سطح + نوار XP در شهر بازی‌ها و کارنامهٔ کودک + جشن ارتقا (صدا + فندقی + کارت طلایی)
- لحن ضداعتیاد حفظ شد (پیشنهاد تنوع پس از ۵ بار تکرار، سقف زمانی والدین)

### ۳. مأموریت‌های روزانهٔ جدید 🎯
`words` (کلمه بساز ×۲)، `counting` (بشمار و مرتب کن ×۲)، `logic` (کارآگاه منطق ×۲) — مجموع مأموریت‌ها: ۹؛ صندوق جایزهٔ روزانه با همان منطق امن قبلی.

### ۴. مدال‌های جدید 🏅 (+۱۴، مجموع ۶۶)
مسیر XP (دوست فندقی/هم‌بازی/کاشف جزیره/قهرمان/افسانه) + استاد اعداد + استاد کلمه‌ها + استاد شکل‌ها + استاد رنگ‌ها + کارآگاه کوچک + استاد منطق — همه در جعبهٔ مدال‌های موجود قابل مشاهده.

### ۵. جزیرهٔ زنده‌تر 🌅
- چرخهٔ روز **چهاره‌فازی**: صبح / ظهر / **غروب طلایی (۱۷–۱۹)** / شب — با `cycleForHour` تست‌پذیر
- آسمان گرم غروب + خورشید نارنجی در نقشهٔ جزیره (نقاشی موجود توسعه یافت)
- سکوی هشتم جزیره: **چادر کارناوال** (asset جدید ۵۸KB، پالت ۸بیت، پس‌زمینهٔ شفاف)
- ریتم عمودی سکوها بازچینش شد — بدون حذف هیچ سکو یا مسیری

### ۶. تجربهٔ آپدیت 🚀
- «تازه‌های نسخه» بعد از آپدیت یک‌بار **خودکار** نمایش داده می‌شود و سپس به مقصد اصلی می‌رود (`GrowthStore.shouldShowWhatsNew`)
- محتوای ۷.۰.۰ در `WhatsNew` بازنویسی شد (سه بخش: بازی‌های تازه / سطح جزیره / جزیرهٔ زنده)

### ۷. پنل والدین 👨‍👩‍👧
- محور «هوش و منطق» به رادار مهارتی اضافه شد (۹ محور)
- ردیف «هوش و منطق» به وضعیت یادگیری موضوعات کارنامه اضافه شد
- کارت «سطح جزیره + نوار تجربه» در کارنامهٔ کودک

### ۸. زیرساخت کیفیت
- گیت کیفیت CI (`analyze + test` قبل از Build) در `ci/quality-gate-build-apk.v7.yml` آماده شد (به‌دلیل محدودیت دسترسی توکن اپ به فایل‌های workflow، نیازمند فعال‌سازی دستی/مجوز — جزئیات در پایین)

---

## 🐞 Bugهای رفع‌شده در طول همین نسخه (پیدا شده توسط QA داخلی)

| # | باگ | شدت | رفع |
|---|-----|-----|-----|
| B1 | ریسک حلقهٔ بی‌نهایت در تولید گزینه‌های انحرافی (شمارش وقتی پاسخ=۱ و جمع وقتی نتیجه=۲) | بحرانی | بازنویسی با مجموعهٔ قطعی همسایه‌ها + تست |
| B2 | `FandoghiCoach.incorrect(label)` در بازی‌های جدید، لیبلِ گزینهٔ **اشتباهِ** انتخابی را به‌عنوان «پاسخ درست» اعلام می‌کرد (لو دادن جواب غلط!) | بالا | بازطراحی به بازخورد مثبت بدون لو دادن جواب |
| B3 | خطای محتوایی: «کلاغ‌ها» با ایموجی کفشدوزک 🐞 | متوسط | اصلاح نام به «کفشدوزک» |
| B4 | خرابی خودکار `game_data.dart` هنگام بازگردانی دستی (دو نقطهٔ تخریب) — توسط بررسی توازن توکن‌ها شناسایی شد | بحرانی | بازسازی کامل با ۱۸ جایگزینی assertions دار + تأیید توازن |
| B5 | `PremiumButton.onPressed: null` — ویجت فقط `VoidCallback` می‌پذیرد و null کرش تایپی می‌داد | بالا | `() {}` با متن راهنمای «اول انتخاب کن!» |
| B6 | گم‌شدن ویرایش‌ها در ویرایش‌های موازی هم‌فایل (importهای main.dart، nextRoute و…) | بالا | ممیزی نهایی تک‌تک الگوها با grep شمارشی |
| B7 | تداخل برچسب/پل سکوی هشتم در ریتم قدیمی نقشه | متوسط | بازچینش ریتم عمودی |

---

## ⚡ Performance (قبل ← بعد)

| سنجه | قبل (6.2.6) | بعد (7.0.0) | توضیح |
|------|-------------|--------------|-------|
| Startup | ~بدون تغییر | **بدون تغییر** | XP از همان اسنپ‌شات Hive خوانده می‌شود؛ صفر I/O جدید؛ محاسبهٔ سطح در حافظه (O(1)) |
| RAM | سقف imageCache 48MB/80 | همان | بازی‌های جدید ایموجی‌محور — بدون decode تصویر |
| CPU/GPU | — | قابل چشم‌پوشی | فقط یک انیمیشن پالس Hint (تایمر ۱۰ ثانیه‌ای، off وقتی لازم نیست) |
| حجم | — | **+~۶۰KB asset، +~۱۲۰KB کد تخمینی** | تنها asset جدید: کارناوال ۵۸KB (پالت ۸بیت)؛ صفر صدا/فونت/کتابخانهٔ جدید |
| وابستگی‌ها | — | **صفر تغییر** | هیچ پکیج جدیدی اضافه نشد |

---

## 🧪 تست‌ها

| دسته | تعداد | وضعیت |
|------|-------|-------|
| تست‌های جدید نسخه ۷ | **۴۱** | ✍️ نوشته‌شده |
| — ریاضیات XP (خالص) | ۵ | |
| — ثبت XP در GameData + مدال‌ها | ۹ | |
| — **مهاجرت Hive از 6.2.6** | ۳ | بذر XP، حفظ xp جدید، سقف ۵۰۰۰ |
| — مولدهای ۱۰ بازی (هر بازی) | ۱۶ | پاسخ درست یکتا، بدون گزینهٔ تکراری، درستی محتوایی |
| — ویجت چارچوب بازی | ۴ | جریان کامل، مأموریت/XP، Hint، دور جدید |
| — چرخهٔ روز + مأموریت‌ها + موسیقی | ۷ | |
| — جریان آپدیت (integration) | ۱ | |
| قراردادهای تست موجود به‌روز شد | ۲ | (۶۶ مدال، ۹ محور رادار) |
| **کل مجموعه تست پروژه** | **۲۷۸** | |

**نتیجهٔ اجرا:** ⚠️ **اجرا نشده** — به‌دلیل دو مسدودکنندهٔ محیطی (بند «Release Readiness»). کد از نظر ساختاری (توازن، ایمپورت‌ها، قراردادها) بازبینی دستی کامل شده است.

---

## 📱 Compatibility

- minSdk 21 (Android 5.0) / targetSdk 35 / compileSdk 35 — **بدون تغییر**
- Flutter 3.24.3 (همان CI) — بدون تغییر وابستگی
- RTL کامل، ارقام فارسی همه‌جا (`PersianDigits`)، فونت‌های باندل آفلاین
- تست واقعی روی دستگاه: ❌ نیازمند Build (بند بعدی)

---

## 🔒 امنیت

- **صفر Secret/Token/Key جدید** در مخزن (تأیید)
- داده‌های جدید ۱۰۰٪ Local (Hive) — هیچ شبکه‌ای برای XP/بازی‌ها
- بازی‌های پریمیوم پشت `GameAccessGate` + PIN والدین (PBKDF2) — دکمهٔ خرید فقط در بخش محافظت‌شده
- سیستم خرید/Restore **دست‌نخورده** (تأیید Audit)
- ساعت خواب و فیلتر محتوای والدین روی همهٔ مسیرهای جدید اعمال می‌شود
- ساختار ضد Tamper و Keystore بدون تغییر

## 🔄 Migration (6.2.6 → 7.0.0)

- معماری ذخیره‌سازی نسخه‌دار موجود + `schema_version` بدون تغییر؛ فیلدهای جدید فقط با پیش‌فرض
- کلید `xp`: اسنپ‌شات‌های قدیمی ندارند → بذر خودکار `min(5000, stars×5 + coins)` — پیشرفت قبلی محترم
- Export/Import والدین کلید `xp` را شامل می‌شود؛ پروفایل خواهر/برادر جدید XP صفر می‌گیرد
- **۳ تست مهاجرت Hive** با دادهٔ شبیه‌سازی‌شدهٔ 6.2.6 نوشته شد (اجرای آن‌ها مشمول مسدودکنندهٔ بالا)

---

## 🚦 Release Readiness

# ⛔ NOT READY

**تنها و تنها به‌دلیل دو مسدودکنندهٔ بیرونی (غیر از کد):**

### مسدودکنندهٔ ۱ — اجرای CI ناممکن است (خطای BILLING اکانت GitHub)
اجرای Actions این ریپو با پیام رسمی زیر رد می‌شود (job قبل از هر step):
> *"The job was not started because recent account payments have failed or your spending limit needs to be increased."*

بنابراین **Build و Compile تأیید نشده** (و در سندباکس نیز Flutter قابل نصب نیست — pub.dev و Google Storage مسدودند).

**رفع:** تنظیمات GitHub → Billing & plans → پرداخت مانده/افزایش Spending Limit.

### مسدودکنندهٔ ۲ — گیت کیفیت CI (analyze+test) نمی‌تواند فعال شود
توکن GitHub App این نشست مجوز `workflows` ندارد (Push فایل workflow رد شد). فایل آماده در `ci/quality-gate-build-apk.v7.yml` قرار داده شد.

**رفع (یکی از دو راه):** (الف) اعطای مجوز workflows به اپ Arena در تنظیمات ریپو، یا (ب) کپی دستی فایل فوق به `.github/workflows/build-apk.yml`.

### پس از رفع هر دو مسدودکننده (خودکار):
1. push سبز CI: `flutter analyze` + `flutter test` (۲۷۸ تست) + `flutter build apk/aab` + تأیید امضا
2. تأیید مهاجرت روی دستگاه واقعی: نصب 6.2.6 → آپدیت → بررسی ستاره/سکه/مرحله + XP بذری + نمایش تازه‌های نسخه
3. تست خرید/Restore در مایکت
4. در صورت سبزشدن همه: **READY FOR RELEASE**

---

## 🧭 نقشهٔ تحویل

- شاخه: `arena/01a0c3ee-jazire-fandoqi` (۲ کامیت، ۴۷ فایل، +۳,۹۰۰ خط)
- **PR #151**: https://github.com/Parsa-apps/Jazire-Fandoqi/pull/151
- ممیزی کامل: `AUDIT_V7.md` | این گزارش: `RELEASE_REPORT_V7.md` | گیت کیفیت آماده: `ci/quality-gate-build-apk.v7.yml`
