# 📋 گزارش ممیزی حرفه‌ای پروژه Flutter — «جزیره فندقی» (kudake_iran)

**تاریخ ممیزی:** ۱۳ اوت ۲۰۲۶ (۲۲ مرداد ۱۴۰۵)
**محدوده:** کل مخزن GitHub (`farshadkurd/kudake_iran`) — commit `616b7f8` روی `main`
**نوع بررسی:** Code Review / معماری / کارایی / حجم / UI-UX کودک / رسانه / انتشار / کیفیت کد
**وضعیت:** فقط گزارش — هیچ تغییری در کد ایجاد نشده است.

---

## ۰. خلاصه مدیریتی (Executive Summary)

| حوزه | امتیاز از ۱۰ | وضعیت کلی |
|---|---|---|
| معماری | ۵٫۵ | ساختار feature-first خوب شروع شده اما با «گود کلاس» استاتیک و معماری دوگانه ناقص مانده |
| کارایی (Performance) | ۶ | بهینه‌سازی‌های هوشمندانه‌ای وجود دارد ولی استارتاپ سریالی، انیمیشن‌های بی‌وقفه و نبود RepaintBoundary مشکل‌ساز است |
| حجم APK | ۵ | زیرساخت R8/Shrink عالی است ولی CI خروجی Fat می‌دهد و WAV و آیکون‌های ۱٫۴MB بیهوده حمل می‌شوند |
| UI/UX کودک | ۷٫۵ | بهترین بخش پروژه؛ Touch Target ۶۴px، صداگذاری ملایم، دسترس‌پذیری — اما عمق ناوبری و Paywall برای کودک ۳ ساله زیاد است |
| مدیریت رسانه | ۷ | استخر پلیر و Debounce حرفه‌ای است؛ فرمت WAV و نبود کش دیسک برای کارتون‌ها نقطه ضعف است |
| آمادگی انتشار | ۴ | تنظیمات Release حرفه‌ای است اما **نشت Keystore در مخزن عمومی** یک بحران امنیتی واقعی است |
| کیفیت کد | ۶ | کامنت‌گذاری و مدیریت خطای عالی؛ کد تکراری، فایل‌های ۱۵۰۰ خطی و وابستگی‌های اضافی دارد |
| مقیاس‌پذیری آینده | ۵ | افزودن هر بازی جدید نیازمند دست‌کم ۵ تغییر در نقاط مختلف است |

### 🔴 مهم‌ترین یافته (باید همین امروز اقدام شود)
فایل‌های `android/key.properties` (شامل **رمز عبور به‌صورت متن ساده**) و `android/release.keystore` در یک **مخزن عمومی GitHub** commit شده‌اند. هر کسی در اینترنت می‌تواند با این کلید، APK امضاشده به نام توسعه‌دهنده بسازد. جزئیات در بخش C1.

### آمار کلی پروژه
- **۱۸۱ فایل Dart** — حدود **۴۸٬۹۰۰ خط کد**
- **۲۴MB دارایی** (assets): ۱۲۳ تصویر WebP، ۹۳ فایل MP3، ۴۳ فایل WAV، ۲ فونت TTF، ۲ PNG بزرگ
- **۲۲ فایل تست** (شروع خوب، اما بدون اجرا در CI)
- ۱۶ بازی + آکادمی‌های محتوا، داستان، لالایی، کارتون آنلاین، پنل والدین
- وابستگی‌ها: `flutter_riverpod`، `just_audio`، `flame`، `hive`، `video_player`، `flutter_tts`، `google_fonts`، `cryptography`، `talker`، …

---

## ۱. معماری پروژه

### ۱.۱ ساختار پوشه‌ها
```
lib/
├── app/          (تم، فونت، توکن‌های طراحی) ✅
├── core/         (سرویس‌ها و داده‌های محتوایی) ⚠️ بسیار شلوغ
├── data/         (datasources + repositories) ✅ ساختار درست
├── domain/       (entities, repositories, usecases) ✅ ساختار درست
├── features/     (۲۸ فیچر: بازی‌ها، داستان، کارتون، رشد، …) ✅ feature-first
├── presentation/ (providers) ⚠️ تقریباً استفاده‌نشده
└── shared/widgets/ ⚠️ دارای فایل‌های تکراری
```

**نقاط قوت:**
- ساختار `features/` بر اساس فیچر است — استاندارد و قابل دفاع.
- لایه‌های `domain` (شامل `PlayerProfile`, `GameStage`, چهار UseCase و ریپازیتوری) و `data` طبق Clean Architecture ساخته شده‌اند.
- جداسازی `app/` برای Design Tokens و Theme حرفه‌ای است.
- وجود `shared/widgets` برای کامپوننت‌های مشترک (ChildTouchTarget، GlassCard و…).

**نقاط ضعف:**
- **معماری دوگانه و ناقص:** لایه‌های Clean Architecture فقط برای «پروفایل بازیکن» سیم‌کشی شده‌اند (`player_profile_provider`, `use_case_providers`). اما بدنه اصلی اپ روی کلاس استاتیک `GameData` می‌چرخد که در **۷۱ فایل** استفاده می‌شود، در حالی که فقط **۹ فایل** از Riverpod (`ConsumerWidget/ref.watch`) استفاده می‌کنند. عملاً Riverpod یک وابستگی نیمه‌مرده است.
- **God Class:** فایل `lib/core/game_data.dart` با **۱۵۲۰ خط و ۱۶۵ عضو استاتیک** همه‌چیز را در خود دارد: وضعیت بازیکن، اقتصاد (سکه/ستاره)، مأموریت‌ها، دستاوردها، تنظیمات، پیشرفت مراحل و منطق ذخیره‌سازی. این کلاس «منبع حقیقت» است اما تست‌پذیری و نگهداری آن بسیار سخت است.
- **فایل‌های غول‌پیکر:** ۹ فایل بالای ۱۰۰۰ خط که UI + منطق + داده را باهم دارند:
  - `cartoon_player_screen.dart` (۱۵۴۲ خط)
  - `game_data.dart` (۱۵۲۰)، `cartoon_data.dart` (۱۲۳۸)، `dashboard_tab.dart` (۱۲۰۹)
  - `parent_panel.dart` (۱۱۹۵)، `story_reader_screen.dart` (۱۱۹۴)
  - `learning_quiz_game.dart` (۱۰۵۸)، `cartoon_hub_screen.dart` (۱۰۴۰)، `profile_screen.dart` (۱۰۱۴)
- **main.dart با ۶۳۴ خط** هم جدول route است، هم دیسپچر بازی، هم خطایابی سراسری. تابع `_gameFor()` بازی را با `name.contains('الفبا')` روی رشته‌های فارسی پیدا می‌کند — شکننده، غیرقابل تست و غیرمقیاس‌پذیر.
- **عوارض جانبی در builder:** داخل `_gameFor()` (که یک widget builder است) کارهایی مثل `ActivityTracker.recordOpen` و `GameData.recordGamePlayed` انجام می‌شود؛ یعنی ساخت ویجت با تغییر وضعیت همراه است — یک ضدالگوی معماری.
- **نبود Dependency Injection:** همه سرویس‌ها استاتیک هستند (`AudioService`, `GameData`, `Monetization`, `FandoghiCoach`, …)؛ mock کردن برای تست تقریباً ناممکن است.
- **سه سامانه ذخیره‌سازی موازی:** SharedPreferences + Hive + EncryptedSharedPreferences (Keystore). هرکدام برای بخشی از داده‌ها — پیچیدگی بی‌مورد.

### ۱.۲ مقیاس‌پذیری برای امکانات آینده
افزودن یک بازی جدید امروز نیازمند تغییر در **حداقل ۵ نقطه** است: import و route table در `main.dart`، تابع `_gameFor`، لیست داشبورد، داده‌های نقشه مراحل (`game_data.dart`) و گاهی `GameAccessGate`. **نتیجه: مقیاس‌پذیری ضعیف.** تا زمانی که routeها typed (مثلاً با go_router) و state به Riverpod مهاجرت نکند، هر فیچر جدید هزینه فزاینده دارد.

---

## ۲. کارایی (Performance)

### ۲.۱ کندی استارت (Cold Start) — مهم‌ترین مشکل سرعتی
در `main()` قبل از `runApp` این کارها **به‌صورت سریالی** await می‌شوند:
```
Hive.initFlutter() → openBox() → AudioService.init() (شامل ستاپ TTS)
→ GameData.load() → GrowthStore.load() → SecurityHardeningService.assess()
```
روی گوشی‌های ضعیف ایرانی (۱ تا ۲ گیگ RAM)، این زنجیره می‌تواند **۱ تا ۳ ثانیه** صفحه سفید/launch اضافه کند. ارزیابی امنیتی (بررسی امضا، Frida، root) در release حتماً باید **بعد از اولین فریم** اجرا شود، نه قبل از runApp.

### ۲.۲ Rebuildهای غیرضروری
- **۲۵۸ فراخوانی `setState`** در پروژه؛ چندین مورد `setState(() {})` خالی (مثلاً `dashboard_tab.dart` خطوط ۴۵۷ و ۴۸۴) که کل زیردرخت داشبورد ۱۲۰۰ خطی را بازسازی می‌کند.
- `GameData.changes` یک `ValueNotifier` سراسری است؛ هر تغییری (حتی افزودن یک سکه) همه Listenerها را بیدار می‌کند. الگوی درست (selective watch) با Riverpod ممکن بود که استفاده نشده.
- در مقابل، استفاده خوب از `ValueListenableBuilder` و `AnimatedBuilder` در ویجت‌های کوچک دیده می‌شود؛ مشکل در صفحات بزرگ است نه کل پروژه.

### ۲.۳ انیمیشن‌ها
- **۴۸ AnimationController** در پروژه؛ چندین کنترلر با `repeat(reverse: true)` بی‌وقفه (شناور شدن سکوها در داشبورد، موج آب در جزیره، ستاره‌ها).
- فقط **۵ مورد RepaintBoundary** در کل پروژه وجود دارد! انیمیشن‌های مداوم بدون RepaintBoundary، هر فریم کل subtree را repaint می‌کنند → افت FPS و مصرف باتری روی GPUهای ضعیف.
- نقاط قوت: استفاده از `flutter_animate` و CustomPainterهای سبک (جزیره به‌جای تصاویر سنگین paint می‌شود)؛ `AssetManager.filterQuality` برای گوشی ضعیف قابل کاهش است.

### ۲.۴ مصرف RAM و تصاویر
- کلاس `AssetManager` یک LRU دستی روی `ImageProvider` ساخته است، اما **کش واقعی تصاویر decode شده در `ImageCache` موتور Flutter است** — کش کردن آبجکت provider عملاً حافظه آزاد نمی‌کند. بودجه پیش‌فرض ImageCache فلاتر **۱۰۰MB** است که برای گوشی ۱GB RAM زیاد است و هیچ‌جا `PaintingBinding.instance.imageCache.maximumSizeBytes` تنظیم نشده.
- نقاط قوت: `cacheWidth` در ۹ نقطه استفاده شده (درست)، فرمت WebP برای همه تصاویر (عالی)، `precacheImage` محدود و هدفمند.
- ۲۵ مورد `Image.asset` مستقیم بدون errorBuilder/cacheWidth وجود دارد که از سیستم یکپارچه خارج‌اند.

### ۲.۵ تایمرها
`_PlayTimeTracker` یک تایمر **۱ ثانیه‌ای دائمی** + یک تایمر **ذخیره خودکار هر ۱۰ ثانیه** دارد که **حتی اگر هیچ داده‌ای تغییر نکرده باشد** روی دیسک می‌نویسد (`GameData.save()` و `GrowthStore.save()`). روی فلش‌های ضعیف eMMC، این I/O مداوم هم مصرف باتری دارد و هم می‌تواند باعث micro-jank شود.

### ۲.۶ کارتون‌های آنلاین
`AparatService` برای هر پخش یک درخواست HTTP با timeout ۱۸ ثانیه می‌زند (کش درون‌حافظه‌ای ۲ ساعته دارد). روی اینترنت ضعیف، کودک تا ۱۸ ثانیه spinner می‌بیند. کش فقط در RAM است و با بستن اپ از بین می‌رود.

---

## ۳. حجم APK

### ۳.۱ تحلیل دارایی‌ها (۲۴MB)
| بخش | حجم | وضعیت |
|---|---|---|
| `assets/audio` (صداها) | ۸٫۵MB | ⚠️ ۲٫۹MB آن WAV فشرده‌نشده است |
| `assets/stories` | ۳٫۳MB | ✅ WebP |
| `assets/cartoons` (پوسترها) | ۳٫۰MB | ✅ WebP |
| `assets/premium` | ۲٫۳MB | ✅ WebP |
| `assets/icons` | ۱٫۴MB | 🔴 فقط ۲ فایل = ۱٫۳۷MB! |

### ۳.۲ یافته‌های کلیدی کاهش حجم
1. **🔴 آیکون‌های ۱٫۴ مگابایتی:** `app_icon.png` (۷۱۱KB) و `app_icon_foreground.png` (۶۶۵KB) هم در mipmapهای اندروید هستند و هم به‌عنوان asset فلاتر در APK کپی می‌شوند. این دو فقط خوراک `flutter_launcher_icons` هستند و در ران‌تیم مصرف ندارند → **~۱٫۳۷MB حذف رایگان.**
2. **🟠 فایل‌های WAV:** `assets/audio/sfx` (۷۷۶KB) و `assets/audio/learning` (۲٫۱MB) فرمت WAV دارند. تبدیل به **OGG/Opus** (که `just_audio` کامل پشتیبانی می‌کند) با کیفیت برابر، **۷۰ تا ۸۵٪ کاهش** می‌دهد → حدود **۲٫۵MB صرفه‌جویی.**
3. **🔴 CI خروجی Fat می‌دهد:** در `build-apk.yml` دستور `flutter build apk --release` بدون `--split-per-abi` اجرا می‌شود؛ یعنی کتابخانه‌های native هر سه ABI (arm64 + armv7 + x86_64) در یک APK قرار می‌گیرند. اسکریپت `build_optimized.sh` (با split + obfuscate) وجود دارد ولی **CI از آن استفاده نمی‌کند** → احتمالاً ۱۵ تا ۲۰MB حجم بی‌مورد. برای کافه‌بازار باید APK به‌تفکیک ABI یا AAB آپلود شود.
4. **وابستگی‌های سنگین کم‌مصرف:**
   - `flame` (موتور بازی) فقط در **۲ بازی از ۱۶ بازی** استفاده می‌شود.
   - `google_fonts` با وجود فونت لوکال Vazirmatn در pubspec؛ عملاً همه‌جا fallback لوکال استفاده می‌شود و wrapper پر از try/catch است.
   - `talker` فقط برای لاگ debug؛ در release هم داخل باینری می‌ماند.
5. **نقاط قوت موجود:** `minifyEnabled + shrinkResources + R8 fullMode` فعال، `resConfigs "en","fa"`، اسپلیت‌های AAB فعال، `enableR8.fullMode=true` در gradle.properties — زیرساخت درست است، فقط پایپ‌لاین نهایی ناقص است.

**برآورد:** با اجرای موارد ۱ تا ۳، حجم APK نهایی (هر ABI) احتمالاً **۳۰ تا ۴۰٪** کاهش می‌یابد، بدون هیچ افت کیفیت ظاهری.

---

## ۴. UI/UX از دید کودک ۳ تا ۸ سال

### نقاط قوت (واقعاً قابل تقدیر)
- **هدف لمسی ۶۴×۶۴ پیکسل** با ویجت اختصاصی `ChildTouchTarget` (استاندارد حرکتی کودک) + `ChildPressable` با بازخورد scale.
- **بازخورد چندحسی:** لمس = ویبره سبک + صدای ملایم (۰٫۳۶–۰٫۴۰ ولوم)؛ صدای خطا هرگز بلند/ترسناک نیست (۰٫۳۸–۰٫۴۴).
- **شخصیت «فندقی»:** مربی با حافظه (اسم کودک را به یاد می‌آورد)، پیام‌های تشویقی، پیشنهاد تنوع بعد از ۵ بار بازی تکراری (ضد اعتیاد).
- **دسترس‌پذیری:** بررسی کوررنگی، تنظیم کنتراست WCAG AA، برچسب‌های Semantics فارسی، مقیاس فونت قابل تنظیم والد، حالت چپ‌دست، ارقام فارسی سراسری.
- **قابلیت‌های والد:** پین ۴ رقمی (هش‌شده، هرگز خام ذخیره نمی‌شود)، ساعت خواب، محدودیت زمان، مسدودسازی مسیرها، FLAG_SECURE روی صفحات حساس.
- **آفلاین بودن** = بدون spinner برای کودک در شرایط رایج اینترنت ایران.

### نقاط ضعف و ریسک‌ها
1. **عمق ناوبری زیاد برای ۳–۴ ساله‌ها:** splash → onboarding → gateway (سه دنیای یادگیری/بازی/کاوش) → هاب → بازی → مرحله. با ۱۶ بازی + آکادمی‌ها + جزیره + فروشگاه، کودک ۳ ساله در انتخاب غرق می‌شود. پیشنهاد: صفحه خانه مبتنی بر سن (۳–۴ فقط ۴ دکمه بزرگ؛ ۵–۶ نسخه کامل‌تر).
2. **Paywall در مسیر کودک:** وقتی کودک روی بازی قفل‌شده می‌زند، اول Paywall می‌بیند (`GameAccessGate`)؛ درک «خرید» برای کودک وجود ندارد و تجربه با بن‌بست قطع می‌شود. بهتر است قفل‌ها همیشه از نظر بصری مشخص باشند (قفل روی کاشی) و paywall فقط به والد نشان داده شود.
3. **اقتصاد چندگانه پیچیده:** سکه، ستاره، مأموریت روزانه، چرخ شانس، استیکر، صندوقچه — برای زیر ۶ سال مفاهیم انتزاعی زیادی است.
4. **پوشش ناقص استاندارد لمس:** `ChildTouchTarget` فقط در **۲۱ نقطه** استفاده شده در حالی که صدها دکمه/کاشی در اپ هست؛ بقیه دکمه‌ها به اندازه بودنشان تضمینی نیست.
5. **ریسک ایمنی محتوا در کارتون‌ها:** اگر resolve لینک آپارات شکست بخورد، سرویس **عنوان را جستجو می‌کند و اولین نتیجه را پخش می‌کند** — برای یک اپ کودک، پخش ویدیوی نامرتبط/نامناسب یک ریسک ایمنی جدی است (جزئیات در C2).
6. **مسکات قابل درگ** (`DraggableFandoghi`) می‌تواند روی دکمه‌های اصلی بیفتد و کودک نداند چرا لمس کار نمی‌کند.

---

## ۵. مدیریت صدا و فایل‌های رسانه‌ای

### نقاط قوت
- **استخر ۸ پلیری** برای SFX تا صدای جشن با صدای کلیک قطع نشود + Debounce ۴۵ms برای تپ‌های پشت سر هم.
- تفکیک صحیح session صوتی: داستان‌ها و لالایی‌ها از `StoryAudioService` جدا مدیریت می‌شوند و SFX آن‌ها را قطع نمی‌کند.
- TTS فارسی فقط fallback برای محتوای پویاست؛ تلفظ حروف/اعداد/رنگ‌ها از فایل‌های ضبط‌شده آفلاین است (درست‌ترین تصمیم برای بازار ایران که موتور TTS فارسی روی خیلی گوشی‌ها نیست).
- ولوم‌های دسته‌بندی‌شده و مستند در `sfxVolumes`.

### نقاط ضعف
1. **فرمت WAV** برای sfx و صداهای آموزشی (۲٫۹MB فشرده‌نشده در APK + decode سنگین‌تر). → تبدیل به OGG/Opus.
2. **نبود preload:** هر بار `setAsset` روی پلیر انجام می‌شود؛ اولین تپ هر صدا کمی تأخیر دارد. صداهای داغ (tap/click/correct) باید در استارت preload شوند.
3. **کد مرده:** `_bgmPlayer` ساخته شده ولی BGM عمداً خاموش است؛ متدهای `playBgm/setBgmVolume` خالی‌اند → حذف یا نگهداری با کامنت شفاف‌تر.
4. **کارتون‌ها فقط استریم** هستند؛ هیچ کش دیسکی (پوستر یا ویدیو) وجود ندارد. برای استفاده آفلاین پیشنهاد: «بسته‌های دانلود» (Download Packs) با مدیریت حافظه و انقضا، که والد انتخاب کند کدام کارتون‌ها دانلود شوند.
5. **داستان‌ها/لالایی‌ها MP3** هستند (جمعاً ~۵MB) — برای صدای گفتار مونو، re-encode با بیت‌ریت ۶۴kbps حجم را نصف می‌کند بدون افت محسوس.

---

## ۶. آمادگی انتشار در فروشگاه‌ها

### نقاط قوت (قابل توجه)
- `targetSdk = 35`, `compileSdk = 35` — به‌روز.
- R8 fullMode + ProGuard rules + shrinkResources.
- `network_security_config` بدون cleartext، `allowBackup=false`، `dataExtractionRules`.
- مجوزها حداقلی‌اند و هیچ مجوز گالری، تصویر، دوربین یا حافظهٔ خارجی درخواست نمی‌شود.
- پرداخت کافه‌بازار با Poolakey + تأیید امضای RSA + تشخیص sandbox.
- Anti-tamper: بررسی امضای APK در ران‌تیم (SHA-256 گواهی در BuildConfig)، تشخیص Frida/Xposed/debugger با سیاست معقول (فقط tamper قطعی مسدود می‌شود؛ root شبیه‌ساز فقط ثبت می‌شود).
- کرش‌ها به‌صورت آفلاین در Hive ذخیره و در پنل والد قابل مشاهده‌اند؛ `ErrorWidget.builder` مانع صفحه سفید می‌شود.

### نقاط ضعف
1. **🔴 بحرانی: نشت کلید امضا** (بخش C1) — عملاً انتشار امن را بی‌معنی می‌کند.
2. **CI هیچ تستی اجرا نمی‌کند:** در `build-apk.yml` نه `flutter analyze` هست نه `flutter test`. اسکریپت محلی `build.sh` هر دو را دارد ولی پایپ‌لاین گیت‌هاب از آن استفاده نمی‌کند. یعنی یک PR شکسته می‌تواند APK بسازد.
3. **نام‌گذاری آرتیفکت CI قدیمی است:** `kudake_iran_v3.0.0_apk` در حالی که نسخه واقعی `6.2.0+13` است.
4. **Flutter پین‌شده روی `3.24.3`** (سپتامبر ۲۰۲۴) — در اوت ۲۰۲۶ یعنی ~۲ سال عقب‌تر از stable. وابستگی قدیمی انتخاب و برش عکس حذف شده؛ کامنت NDK همچنان نیازمند بازبینی در ارتقای بعدی است. هرچه این بدهی بزرگ‌تر شود، مهاجرت سخت‌تر می‌شود.
5. **`--obfuscate --split-debug-info` در CI نیست** (فقط در `build_optimized.sh` محلی) → کرش‌های release قابل symbolicate نیستند و باینری بدون obfuscation توزیع می‌شود.
6. **گزارش کرش صرفاً محلی است:** هیچ ابزار remote (مثل Sentry) وجود ندارد؛ در تولید از کرش‌های واقعی بی‌خبر می‌مانید.
7. فایل `run_log.txt` (خروجی شکست CI) در مخزن commit شده — باید حذف شود.

---

## ۷. کیفیت کدنویسی

### نقاط قوت
- کامنت‌گذاری فارسی غنی و مستندسازی تصمیم‌ها (نادر و ارزشمند).
- **صفر** TODO/FIXME باز؛ ۴۰۵۷ استفاده از `const` (کاهش rebuild).
- مدیریت خطا فراگیر: تقریباً هر IO و پخشی try/catch و fallback دارد.
- ۲۲ فایل تست برای core و فیچرها + پوشه integration_test.
- `analysis_options.yaml` شامل rules حیاتی مثل `cancel_subscriptions` و `use_build_context_synchronously`.

### مشکلات
1. **کد تکراری:**
   - `glass_card.dart` در دو مسیر (`shared/widgets/` و `shared/widgets/premium/`) با hash متفاوت.
   - `particle_celebration.dart` دو نسخه.
   - `premium_animations.dart` در `core` (۱۴۶ خط) و `shared/widgets/premium` (۶۰۲ خط).
   - `fandoghi_coach` و `fandoghi_welcome` هم در core هم در shared.
   - تابع `_normalizeDigits` داخل dashboard در حالی که `core/growth/persian_digits.dart` وجود دارد.
   → ریسک drift: دو نسخه به‌تدریج متفاوت می‌شوند.
2. **وابستگی‌های اضافی/نیمه‌مرده:** `flame` (فقط ۲ بازی)، `google_fonts` (همه‌جا fallback لوکال)، `talker` (در release غیرفعال ولی حمل می‌شود).
3. **ضدالگوی تطبیق رشته فارسی:** مسیریابی و تشخیص بازی با `contains('الفبا')` — تغییر یک نام بازی، منطق را می‌شکند؛ هیچ کامپایلری آن را نمی‌گیرد.
4. **فایل‌های ۱۰۰۰–۱۵۰۰ خطی** (فهرست بخش ۱.۱) — بازبینی و تست این‌ها عملاً ممکن نیست.
5. **۱۷ فایل Markdown در ریشه مخزن** (مستندات فازها) — باید به `docs/` منتقل شوند؛ ریشه مخزن فقط README و فایل‌های پیکربندی.
6. **lint ضعیف:** فقط ۵ قاعده اضافه؛ قواعدی مثل `always_use_package_imports`, `unawaited_futures`, `avoid_dynamic_calls` توصیه می‌شود.

---

## ۸. مسیر تبدیل به پلتفرم حرفه‌ای آموزش کودک

1. **مهاجرت State:** بازنشستگی تدریجی `GameData` استاتیک به Riverpod (Notifierهای async) + ریپازیتوری‌ها. از حوزه‌های کوچک (مأموریت‌ها، اقتصاد) شروع شود.
2. **مسیریابی typed:** جایگزینی route table فعلی با `go_router` + guardهای والد/پرداخت به‌صورت middleware؛ حذف کامل تطبیق رشته فارسی با route registry مبتنی بر enum/id.
3. **محتوا از کد جدا شود:** داستان‌ها، سؤالات کوییز و داده آکادمی‌ها امروز در فایل‌های Dart کامپایل‌شده‌اند (`children_stories_data.dart` ۹۰۳ خط). مهاجرت به JSON + «بسته‌های محتوای قابل دانلود» یعنی: (الف) کاهش حجم نصب پایه، (ب) انتشار محتوای جدید بدون آپدیت اپ، (ج) پایه یک CMS.
4. **ویدیوی دارای مجوز/خودمیزبان:** وابستگی به scraping آپارات هم شکننده است و هم ریسک ایمنی محتوا دارد؛ فهرست سفید ویدیوها با CDN داخلی یا دانلود آفلاین جایگزین شود.
5. **Observability:** کرش‌ریپورتینگ remote با رعایت حریم کودک (Sentry بدون هیچ PII، فقط stack trace) + متریک‌های کارایی (startup time، frame drops).
6. **CI حرفه‌ای:** analyze + test + coverage gate + بررسی اندازه APK در هر PR (هشدار اگر از آستانه گذشت) + انتشار خودکار با changelog + امضا از Secrets.
7. **تست روی دستگاه ضعیف:** یک integration test با trace زمان استارت و frame budget روی شبیه‌ساز/دستگاه ۱GB RAM به‌عنوان gate کارایی.
8. **مدل درآمدی:** خرید یک‌باره `full_version` پایه خوبی است؛ گام بعدی اشتراک خانوادگی با محتوای ماهانه (بسته‌های فصلی) است — زیرساخت چند پروفایل خواهر/برادر از حالا آماده است.
9. **آموزش تطبیقی:** موتور `weekly_engine` و `adaptive_coach` وجود دارد؛ گام بعدی تشخیص سطح کودک از پاسخ‌ها و تنظیم سختی stages به‌صورت خودکار + تمرین گفتار (تشخیص صدا) برای سنین بالاتر.

---

# 🚨 فهرست مشکلات اولویت‌بندی‌شده

## 🔴 بحرانی (Critical)

### C1 — نشت Keystore و رمز امضا در مخزن عمومی
- **مشکل چیست؟** `android/key.properties` شامل `storePassword` و `keyPassword` به‌صورت متن ساده + فایل `android/release.keystore` در ریپازیتوری **عمومی** GitHub (تأیید شد: `isPrivate=false`) commit شده‌اند. حتی در `.gitignore` کامنت شده که «به درخواست مالک track می‌شوند».
- **چرا مهم است؟** هر کسی می‌تواند APK مخرب را با امضای شما بسازد و در کافه‌بازار/کانال‌های تلگرامی به‌جای اپ اصلی توزیع کند. برای اپ کودک، این فاجعه‌بار است. همچنین CI عمومی شما عملاً با کلید واقعی بیلد می‌دهد و آرتیفکت‌ها در دسترس‌اند.
- **راه‌حل پیشنهادی:**
  1. همین امروز مخزن را **خصوصی** کنید (اقدام فوری).
  2. فایل‌ها را از تاریخ git با `git filter-repo` پاک کنید و فورس‌پوش کنید.
  3. اگر اپ قبلاً با این کلید در کافه‌بازار منتشر شده، با پشتیبانی کافه‌بازار برای چرخش کلید/انتشار مجدد هماهنگ شوید؛ اگر منتشر نشده، یک Keystore جدید بسازید (`tools/generate_release_keystore.sh` موجود است).
  4. رمزها را به GitHub Secrets منتقل کنید؛ `build.gradle` از env بخواند.
- **تأثیر:** جلوگیری از جعل هویت ناشر و محافظت از کاربران کودک. بدون این کار، بقیه اقدامات امنیتی (anti-tamper و…) بی‌اثرند چون مهاجم می‌تواند با کلید واقعی بیلد کند.

### C2 — ریسک ایمنی محتوا در پخش کارتون (fallback جستجو)
- **مشکل چیست؟** در `AparatService.resolve` اگر لینک هش ویدیو پیدا نشود، عنوان کارتون **جستجو** و **اولین نتیجه** برای کودک پخش می‌شود.
- **چرا مهم است؟** در اپ ۳–۸ ساله، پخش ویدیوی نامرتبط یا نامناسب (حتی با احتمال کم) یک حادثه ایمنی و اعتباری است. بعلاوه scraping آپارات با User-Agent جعلی، شکننده و احتمالاً ناقض ToS است و هر لحظه ممکن است از کار بیفتد (۱۸ کارتون همزمان می‌میرند).
- **راه‌حل:** fallback جستجو حذف شود؛ فقط ویدیوهای از پیش resolve و تأییدشده (وایت‌لیست با هش ذخیره‌شده) پخش شوند؛ در صورت شکست، پیام «الان قابل پخش نیست». میان‌مدت: مهاجرت به ویدیوهای خودمیزبان/لایسنس‌دار با دانلود آفلاین.
- **تأثیر:** تضمین «هرگز محتوای نامناسب برای کودک پخش نمی‌شود» + پایداری بخش کارتون.

## 🟠 زیاد (High)

### H1 — دو سامانه State موازی؛ God Class استاتیک
- **مشکل:** `GameData` استاتیک در ۷۱ فایل در برابر Riverpod در ۹ فایل؛ `ValueNotifier` سراسری با notifyهای بی‌مورد.
- **چرا مهم:** rebuildهای غیرهدفمند، race condition در ذخیره‌سازی (با اینکه صف سریال درست شده)، تست‌ناپذیری، و قفل‌شدن معماری برای فیچرهای آینده.
- **راه‌حل:** مهاجرت تدریجی به Riverpod Notifier به‌ازای هر حوزه؛ نگه‌داشتن GameData به‌عنوان لایه persistence صرف؛ حذف Listenerهای دستی.
- **تأثیر:** کاهش rebuild → روان‌تر شدن داشبورد؛ تست‌پذیری؛ توسعه‌پذیری واقعی.

### H2 — استارتاپ سریالی و مسدودکننده
- **مشکل:** ۶ await زنجیره‌ای قبل از اولین فریم (بخش ۲.۱).
- **چرا مهم:** روی گوشی‌های ۱–۲ گیگ بازار هدف، ۱ تا ۳ ثانیه تأخیر = کودک اپ را می‌بندد؛ امتیاز فروشگاه هم آسیب می‌بیند.
- **راه‌حل:** فقط `Hive.init` ضروری قبل از runApp است؛ بقیه (AudioService.init، GrowthStore، SecurityHardening) بعد از اولین فریم با اولویت‌بندی اجرا شوند؛ splash بلافاصله نمایش یابد.
- **تأثیر:** کاهش محسوس cold start (احتمالاً ۴۰–۶۰٪ زمان انتظار).

### H3 — پایپ‌لاین CI ناقص و قدیمی
- **مشکل:** بدون analyze/test، بدون split-per-abi، بدون obfuscate، نام آرتیفکت v3.0.0، Flutter پین‌شده ۳٫۲۴٫۳.
- **چرا مهم:** کیفیت و حجم هر release تصادفی است؛ بدهی نسخه فلاتر رشد می‌کند.
- **راه‌حل:** افزودن `flutter analyze && flutter test --coverage`؛ بیلد `--split-per-abi --obfuscate --split-debug-info`؛ آپگرید پله‌ای فلاتر به stable جاری (هر نسخه یک PR کوچک).
- **تأثیر:** APK کوچک‌تر و امن‌تر؛ جلوگیری از رجعت کیفیت.

### H4 — فایل‌های غول‌پیکر و مسیریابی رشته‌ای
- **مشکل:** ۹ فایل بالای ۱۰۰۰ خط؛ `_gameFor` با `contains('فارسی')`.
- **چرا مهم:** هر تغییر کوچک ریسک شکستن چند بازی را دارد؛ debug و code review کند است.
- **راه‌حل:** استخراج منطق به UseCase/Controller جدا؛ registry مبتنی بر id برای بازی‌ها؛ شکستن صفحات بزرگ به widgetهای کوچک‌تر.
- **تأثیر:** سرعت توسعه بالاتر، کرش کمتر.

### H5 — حجم: آیکون‌های ۱٫۴MB + WAV + Fat APK
- **مشکل:** سه مورد بخش ۳.۲ با هم.
- **چرا مهم:** بازار هدف گوشی‌های ضعیف با حافظه کم است؛ حجم بالا مستقیماً نصب را کم می‌کند (قانون معروف: هر ۶MB افزایش ≈ ۱٪ افت نصب).
- **راه‌حل:** حذف آیکون‌های بزرگ از assetهای فلاتر؛ اسکریپت تبدیل WAV→OGG/Opus (الگوی `tools/optimize_images.py` وجود دارد)؛ بیلد split در CI.
- **تأثیر:** برآورد ۳۰–۴۰٪ کاهش حجم بدون افت کیفیت.

### H6 — انیمیشن‌های مداوم بدون RepaintBoundary
- **مشکل:** ۴۸ کنترلر، چند repeat بی‌وقفه، فقط ۵ RepaintBoundary.
- **چرا مهم:** repaint سراسری در هر فریم → افت FPS و داغ شدن گوشی ضعیف.
- **راه‌حل:** پیچیدن لایه‌های انیمیشنی (آسمان، آب، سکوهای شناور، مسکات) در RepaintBoundary؛ تبدیل انیمیشن‌های صرفاً تزئینی به `Timer`های کم‌فرکانس یا خاموش‌کردن در حالت Low-Power (فلگ `AssetManager.filterQuality` الگوی خوبی است).
- **تأثیر:** FPS پایدار ۶۰ روی میان‌رده‌ها و کاهش مصرف باتری.

### H7 — مدیریت حافظه تصویر برای گوشی‌های ۱GB RAM
- **مشکل:** LRU سفارشی روی ImageProvider بی‌اثر است؛ سقف پیش‌فرض ۱۰۰MB ImageCache دست‌نخورده.
- **چرا مهم:** OOM kill در گوشی‌های ضعیف = کرشی که کاربر فقط «بسته شد» می‌بیند.
- **راه‌حل:** تنظیم `imageCache.maximumSizeBytes` (مثلاً ۴۰–۶۰MB)؛ حذف LRU دستی یا تبدیل به ابزار واقعی evict (با `evictFromMemory`)؛ گسترش `cacheWidth` به همه Image.assetها.
- **تأثیر:** پایداری روی low-end؛ حذف کرش‌های پنهان OOM.

## 🟡 متوسط (Medium)

| # | مشکل | چرا مهم | راه‌حل | تأثیر |
|---|---|---|---|---|
| M1 | ویجت‌های تکراری (glass_card، particle_celebration، premium_animations ×۲) | drift و باگ‌های «در یکی کار می‌کند» | ادغام در یک ماژول واحد + حذف نسخه قدیمی | نگهداری ساده‌تر |
| M2 | تایمر ذخیره هر ۱۰ ثانیه بدون تغییر داده + تایمر ۱ ثانیه‌ای دائمی | I/O و باتری بی‌مورد | ذخیره فقط هنگام dirty بودن (flag)؛ تجمیع تیک‌ها | عمر باتری |
| M3 | بلعیده‌شدن خطاها (`PlatformDispatcher.onError=true`) و نبود کرش‌ریپورت remote | بی‌خبری از مشکلات تولید | Sentry بدون PII + حفظ UX فعلی | دیدپذیری واقعی |
| M4 | سه سامانه ذخیره‌سازی موازی (prefs/Hive/EncryptedPrefs) | پیچیدگی و باگ‌های sync | تجمیع حول Hive + EncryptedPrefs فقط برای حساس‌ها | سادگی |
| M5 | `dependency_overrides` + پین Flutter 3.24.3 | بدهی فنی روبه‌رشد | برنامه آپگرید پله‌ای | آینده‌نگری |
| M6 | پوشش ناقص ChildTouchTarget (۲۱ نقطه از صدها) | خطای لمس برای کودک | ممیزی همه دکمه‌ها و پیچیدن در ChildPressable | UX کودک |
| M7 | نمایش Paywall به کودک + اقتصاد چندگانه | سردرگمی و بن‌بست | قفل بصری از قبل + paywall فقط پشت parental gate | تجربه روان‌تر |
| M8 | lint ضعیف و نبود gate کیفیت در CI | ورود کد با بوی بد | افزودن قواعد + dart_code_metrics | کیفیت پایدار |
| M9 | Anti-tamper: ریسک false positive در آپدیت‌های کافه‌بازار | قفل‌شدن کاربر قانونی | تست روی کانال‌های مختلف نصب + لاگ دلیل بلاک | کاهش پشتیبانی |
| M10 | ۱۷ فایل MD در ریشه مخزن + run_log.txt | شلوغی و نشت اطلاعات CI | انتقال به docs/ و حذف لاگ | نظم |

## 🟢 کم (Low)

| # | مشکل | راه‌حل |
|---|---|---|
| L1 | ناهماهنگی نام‌ها: ریپازیتوری `kudake_iran`، پکیج Dart `jazireh_fandoghi`، applicationId `amoozesh_fandoghi`، نام CI «کودک دانا v3» | یکپارچه‌سازی برند در اسناد و CI |
| L2 | باقی‌مانده‌هایی مثل `assets/audio/numbers/.gitkeep` | حذف |
| L3 | پیکربندی آیکون دو جا (`launcher_icons.yaml` + داخل pubspec) | یکی کردن |
| L4 | `flutter pub run flutter_launcher_icons` در build.sh (نحو قدیمی) | `dart run flutter_launcher_icons` |
| L5 | نبود fastlane/اتوماسیون توزیع داخلی | افزودن در فازهای بعد |
| L6 | کد مرده BGM (`_bgmPlayer` و متدهای خالی) | حذف یا نگهداری با مستندسازی صریح |

---

## ✅ جمع‌بندی نهایی

**این پروژه از نظر «مهندسی محصول کودک» جلوتر از میانگین بازار است:** تصمیم‌هایی مثل آفلاین‌اول بودن، صداهای ضبط‌شده به‌جای TTS، Touch Target کودکانه، پین والد با FLAG_SECURE، ضد اعتیاد و ذخیره کرش محلی — همگی پخته‌اند. مستندات فازبندی‌شده (۱۰۰+ فاز) هم نشان‌دهنده فرآیند توسعه جدی است.

**اما سه اقدام فوری لازم است:**
1. 🔴 **امنیت:** خارج کردن Keystore و رمزها از مخزن عمومی (C1) — قبل از هر انتشار جدید.
2. 🔴 **ایمنی کودک:** حذف fallback جستجوی کارتون (C2).
3. 🟠 **کارایی/حجم:** استارت غیرمسدود (H2) + بیلد split/obfuscated در CI (H3/H5) — هر دو کم‌هزینه و پرتأثیر.

**مسیر میان‌مدت:** مهاجرت تدریجی State به Riverpod و مسیریابی typed (H1/H4)، سپس جدا کردن محتوا از کد برای تبدیل شدن به پلتفرم — این تنها راه افزودن محتوا بدون انتشار نسخه جدید است.

> ⚠️ یادآوری: مطابق درخواست شما، هیچ فایلی در پروژه تغییر نکرده، هیچ commit یا Pull Request ساخته نشده است. این گزارش صرفاً تحلیلی است (خودِ این فایل گزارش، سند جدید است و به کد دست نزده است).
