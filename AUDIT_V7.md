# 📋 گزارش Audit عمیق — پایه نسخه ۷.۰.۰

> تاریخ: ۲ شهریور ۱۴۰۵ / 2026-09-21 — CTO Office
> مخزن: `Parsa-apps/Jazire-Fandoqi` — شاخه کاری: `arena/01a0c3ee-jazire-fandoqi`
> نسخه ممیزی‌شده: **6.2.6+6** (منتشرشده در مایکت)

---

## ۱. ساختار پروژه و معماری

- **فریم‌ورک:** Flutter (SDK ≥3.0)، Dart، Material 3، RTL کامل، `locale: fa`
- **معماری:** Clean Architecture + Feature-First + Riverpod
  - `lib/domain` (entities/usecases/repositories)، `lib/data` (HivePlayerStore، CrashReportStore)،
    `lib/presentation/providers`، `lib/features/*` (۳۰+ فیچر)، `lib/core/*` (سرویس‌های مشترک)، `lib/shared/widgets`
- **حجم:** ۲۲۶ فایل Dart، ~۶۵,۵۰۰ خط کد، ۴۳ فایل تست، ۱۶+ بازی، ۳۰ حیوان، ۱۰ داستان، ۸۰ سؤال مهارت زندگی
- **وابستگی‌ها:** flame، just_audio، flutter_tts، hive، riverpod، google_fonts (با فالبک آفلاین بومی BalooBhaijaan2/Vazirmatn)، video_player، cryptography، talker

**ارزیابی:** معماری سالم و بالغ؛ بدهی فنی کنترل‌شده. الگوی «اسنپ‌شات نسخه‌دار Hive + مهاجرت SharedPreferences» آماده توسعه بی‌خطر است.

## ۲. صحنه‌ها و ناوبری

- دروازه اصلی (`/gateway`) → Splash → Tutorial → Home با ۵ تب: دستاوردها، کوله‌پشتی، **نقشه جزیره**، کارت گزارش، داشبورد بازی‌ها
- ۴۵+ روت ثبت‌شده؛ روت‌های ناشناخته به `_UnknownRouteScreen` می‌روند (رفع باگ قدیمی)
- `GameAccessGate` روی روت‌های پریمیوم + `ParentControls.isRouteBlocked` + `isBedtimeNow`

## ۳. سیستم ذخیره‌سازی (قانون Migration)

- **منبع حقیقت:** Hive `playerBox` → کلید `state_v1` با `schema_version` + کپی `SharedPreferences` برای سازگاری
- خواندن مقاوم: `_readInt/_readString/_readList` با clamp و مقادیر پیش‌فرض؛ `useMemoryFallback` در خرابی کامل
- ذخیره صف‌شده (`_saveQueue`)، جلوگیری از save موازی
- `CrashReportStore`: ۵۰ کرش آخر، کاملاً محلی
- بکاپ/بازیابی AES-256-GCM با پسوند `.parsa` از پنل والدین
- **نتیجه:** افزودن فیلدهای جدید با پیش‌فرض، کاملاً سازگار با نصب‌های 6.2.6 — الگوی `asInt('key', default)` تضمین‌کننده است.

## ۴. خرید درون‌برنامه‌ای

- `BillingService` + `Monetization`: خرید یک‌بارهٔ `full_version`، بدون اشتراک
- Restore: استور مرجع → تأیید امضای RSA رسید → گرنت Keystore؛ آفلاین فقط گرنت Keystore معتبر
- مصرف خریدهای مصرفی قبل از اعطای موجودی؛ دکمه خرید فقط در بخش محافظت‌شده والدین (PIN با PBKDF2 150k)
- **وضعیت:** سالم — نباید دست بخورد. بازی‌های جدید پریمیوم از همان `GameAccessGate` استفاده می‌کنند.

## ۵. پروفایل کودک

- تا ۳ پروفایل (خواهر/برادر) با نام مستعار، ۲۰ آواتار داخلی، عکس کراپ‌شده اختیاری
- بدون حساب کاربری، بدون ارسال داده؛ `sibling_profiles.dart` مستقل از شبکه

## ۶. پنل والدین

- PIN + ساعت خواب + محدودیت زمان روزانه + فیلتر محتوا + گزارش هفتگی + گواهی افتخار + شاخص سلامت رفتاری (`parental_health_radar`) + بکاپ/بازیابی + شفاف‌سازی محتوای آنلاین
- **وضعیت:** غنی و کامل؛ فقط باید آمار XP/بازی‌های جدید در گزارش لحاظ شود.

## ۷. صدا و موسیقی

- `AudioService`: پول ۸ پلیر SFX + پلیر BGM + Voice + صف TTS فارسی، duck خودکار، debounce لمس‌ها
- ۲۱ افکت صوتی با ولوم‌های تنظیم‌شده کودکانه (غلط هرگز بلند نیست)
- BGM مرکزی با `BackgroundMusicObserver` بر اساس روت + `sleep.wav` برای لالایی

## ۸. انیمیشن

- flutter_animate + انیمیشن‌های کنترلر دستی؛ فندقی: ورود/شناور/درگ؛ ConfettiOverlay؛ `RepaintBoundary` در نقشه
- `imageCache` به 48MB/80 عکس محدود شده (گوشی 1GB)

## ۹. بازی‌ها و محتوای آموزشی موجود (که نباید خراب شوند)

الفبا (آکادمی + املای شنیداری)، حافظه، حباب‌ترکان، ستاره‌گیری (Flame)، نقاشی کامل (قلم/پاک‌کن/استیکر/Undo/Redo/آلبوم)، پازل، مسابقه سرعت، صداها، الگوها، آزمون ۹ موضوعه، بدن من، جزیره‌سازی، ۸ بازی ریاضی پایه (چوب‌خط، ارزش مکانی، محور، تقارن، قاب ده‌تایی، تمساح، ساعت، جمع)، ۱۰ دنیای مهارت زندگی، داستان تعاملی ×۱۰، لالایی، کارتون (تنها بخش آنلاین — با اعلام شفاف)، دانشنامه ۳۰ حیوان، SEL، مفاهیم، شغل‌ها.

## ۱۰. Assetها

- وب‌پی‌های بهینه + PNG ماسکوت؛ صداها WAV (SFX) و MP3/M4A؛ فونت باندل‌شده آفلاین
- بازی‌های جدید نسخه ۷ **بدون افزودن asset** (محتوای ایموجی/رنگ بومی) → صفر افزایش حجم بی‌دلیل

## ۱۱. نقاط ضعف شناسایی‌شده (فرصت‌های نسخه ۷)

| # | یافته | شدت | تصمیم |
|---|-------|-----|-------|
| G1 | سیستم «سطح» از سکه مشتق می‌شود (`coins~/100+1`) — سنجه یادگیری نیست، خرید/پاداش آن را می‌پراند | بالا | سیستم XP واقعی «سطح جزیره» اضافه شود |
| G2 | چرخه روز فقط ۳ فاز (صبح/ظهر/شب) — غروب وجود ندارد | متوسط | فاز `sunset` ۱۷–۱۹ |
| G3 | Hint هوشمند فقط در سطح Coach وجود دارد؛ در بازی‌ها تایمر بی‌کاری → راهنمای بصری ندارد | متوسط | Hint ظریف در چارچوب بازی‌های جدید |
| G4 | ۱۰ مکانیزم درخواستی جدید کاملاً غایب‌اند: حرف گمشده، کلمه‌ساز، شمارش اشیا، جمع تصویری، بزرگ/کوچک، جورکردن شکل، ترکیب رنگ، ترتیب اعداد، مورد متفاوت، طبقه‌بندی | بالا | ۱۰ بازی جدید با چارچوب مشترک |
| G5 | مأموریت روزانه ۶ نوع دارد؛ تیپ‌های «کلمه، شمارش، منطق» غایب‌اند | متوسط | افزودن ۳ تیپ مأموریت |
| G6 | مدال‌های XP و مدال تخصصی بازی‌های جدید وجود ندارد | متوسط | ۸ مدال جدید |
| G7 | CI فقط Build می‌کند؛ بدون `flutter analyze`/`flutter test` | بالا | گیت کیفیت در CI |
| G8 | `WhatsNew` هنوز محتوای 6.2.1 را نشان می‌دهد | کم | محتوای 7.0.0 |

## ۱۲. موارد بررسی‌شده و **سالم** (بدون تغییر باقی می‌مانند)

حفظ‌سازی نسخه‌دار، خرید و Restore، پروفایل‌ها، پنل والدین، بکاپ AES، امنیت (Tamper detection، Keystore، PIN PBKDF2، بدون هیچ Secret در مخزن — تأیید شد)، RTL و ارقام فارسی (`persian_digits.dart`)، فونت‌های آفلاین، مدیریت حافظه تصویر،后备 fallback های ذخیره‌سازی، ضداعتیاد (پیشنهاد تنوع بعد از ۵ بار)، مسیر امروز (TodayPath)، پیشنهاد هوشمند آفلاین (`adaptive_coach`)، سازگاری لمس کودک (`child_touch_target`).

## ۱۳. ریسک‌های اجرا و کنترل آن‌ها

- **R1 شکستن Save کاربران 6.2.6** → فیلدهای جدید فقط با پیش‌فرض صفر اضافه می‌شوند؛ تست مهاجرت اسنپ‌شات قدیمی نوشته می‌شود.
- **R2 خطای کامپایل در سندباکس (عدم دسترسی به pub.dev)** → گیت کیفیت `analyze+test` در CI گithub Actions اجرای اجباری قبل از Build؛ همه سوییچ‌های `DayCycle` به‌صورت دستی گشت‌زنی و کامل می‌شوند.
- **R3 کیفیت ۱۰ بازی جدید** → چارچوب مشترک `MiniGameScaffold` + تست واحد مولدهای سؤال + تست ویجت.
- **R4 حجم APK** → بدون asset جدید؛ صفر بایت تصویر/صدای اضافه.

## ۱۴. نتیجه Audit

پروژه در وضعیت **خوب و پایدار** است و آماده پذیرش نسخه بزرگ ۷.۰.۰ به شرط رعایت الگوهای موجود. هیچ قابلیت موجود حذف یا جایگزین نمی‌شود؛ فقط **افزوده** می‌شود.
