## 🔎 بازبینی دور دوم (Deep Pass 2) — ۹ اوت ۲۰۲۶

- فیکس کامپایل: `accessibility.dart` (math.pow)، `audio_service.dart` (dart:async)
- فیکس منطق: کش گزینههای دور در `academy_game` و `math_race` (رفع شافل مجدد)
- فیکس تست: `monetization_test` (سندباکس در تست فعال است)، `child_touch_target_test` (startGesture)
- حذف ۷ import بلااستفاده دیگر + ۱ import dart:ui اضافی
- تأیید: welcome overlay واقعاً mount است، قلبهای ستاره ۵تاست، تمام تستها معتبرند

---
## 🔎 بازبینی کامل ۱۰۰ فاز (QA Review Pass) — ۹ اوت ۲۰۲۶

پس از اجرای تمام فازها، بازبینی خط‌به‌خط انجام شد (گزارش: `docs/PHASE_100_FINAL_REVIEW.md`):

### باگ‌های فیکس‌شده
1. **بحرانی:** تقویم شمسی با الگوریتم Borkowski/jalaali-js بازنویسی شد + ۶ تاریخ مرجع + تست واحد
2. شافل دوباره گزینه‌ها در آزمایشگاه رنگ و الگویاب (کش `_roundOptions`)
3. مخفی‌شدن تکه‌های تکراری پازل ۳×۳
4. مسلح‌نشدن قفل خودکار پنل والدین بعد از ورود PIN
5. بازنشدن مدال‌های ۲۴گانه (اتصال `AchievementSystem.checkAndUnlock` + `recordGamePlayed`)
6. نمایش کارت استراحت چشم برای همه سنین (جداسازی `_buildBreakReminder`)
7. پایان‌پذیری `AI.suggestGames`
8. حذف ۸ import بلااستفاده

### بهبودها
- فونت Vazirmatn آفلاین bundle شد (تکمیل فاز ۷)
- تست واحد تقویم شمسی اضافه شد

---
## فاز ۸۴-۹۰: QA و بهینه‌سازی

- **فاز ۸۴:** مستند تست گوشی ضعیف `docs/PHASE_84_WEAK_PHONE.md`
- **فاز ۸۵:** تست استرس ۵۰۰ عملیات در `game_data_test` + مستند
- **فاز ۸۶:** راهنمای حجم APK `docs/PHASE_86_APK_SIZE.md` (split-per-abi از قبل در build.sh)
- **فاز ۸۷:** Lint/Format — کدها طبق flutter_lints نوشته شدند؛ اجرای واقعی در CI
- **فاز ۸۸:** CI کامل در `docs/PHASE_88_CI.md` (نیازمند مجوز workflows مالک)
- **فاز ۸۹:** پروتکل مصاحبه ۵ والد `docs/PHASE_89_PARENT_INTERVIEWS.md`
- **فاز ۹۰:** قالب گزارش QA نهایی `docs/PHASE_90_QA_REPORT.md`

## فاز ۹۱-۱۰۰: انتشار و رشد

- **فاز ۹۱:** نسخه ۵.۰.۰+۱۰ (pubspec + نمایش در اپ)
- **فاز ۹۲:** مستند Alpha کافه‌بازار `docs/PHASE_92_ALPHA.md`
- **فاز ۹۳:** کمپین لانچ — اسکریپت ویدیو ۳۰ ثانیه‌ای + بنر + متن تلگرام `docs/PHASE_93_LAUNCH_CAMPAIGN.md`
- **فاز ۹۴:** «نظر مامان/بابا» در صفحه درباره ما (دیالوگ بازخورد آفلاین)
- **فاز ۹۵:** داشبورد آنالیتیکس آفلاین `docs/PHASE_95_OFFLINE_ANALYTICS.md`
- **فاز ۹۶:** نقشه راه ۲۰۲۷ `docs/PHASE_96_ROADMAP_2027.md`
- **فاز ۹۷:** تصمیم متن‌باز `docs/PHASE_97_OPEN_SOURCE.md`
- **فاز ۹۸:** جشن کاربران قدیمی در خانه («تو از اول با فندقی بودی!»)
- **فاز ۹۹:** کتابچه والدین PDF واقعی ۹ صفحه‌ای `docs/PHASE_99_PARENT_BOOKLET.pdf` + فونت Vazirmatn در assets/fonts
- **فاز ۱۰۰:** تگ `v5.0.0-irankids` + این گزارش نهایی

---
# 🚀 کودک ایران — نسخه ۵.۰.۰
## جمع‌بندی اجرای ۱۰۰ فاز نقشه راه

تمام ۱۰۰ فاز ROADMAP_100_PHASE اجرا شد:
- ✅ فاز ۱-۱۰: بنیاد (audit، Clean، Riverpod، Hive، Asset، Audio، تم، لاگ، build، docs)
- ✅ فاز ۱۱-۲۰: روانشناسی کودک و UX (فندقی V3، Drag&Drop، داشبورد سنی، Motion، چپ‌دست، دسترس‌پذیری، onboarding تعاملی، راهنمای هوشمند)
- ✅ فاز ۲۱-۳۰: آکادمی محتوا (الفبا V2، اعداد، رنگ+آزمایشگاه، شکل، ۳۰ حیوان، میوه/بدن، شغل/احساس، مفاهیم، ۱۰ داستان، کوییز هوشمند)
- ✅ فاز ۳۱-۴۰: موتور بازی (Memory ۵ سطح + تم ایرانی، Bubble زنجیره‌ای، پازل، ماشین ریاضی، الگویاب، بشنو، بدن، جزیره‌سازی)
- ✅ فاز ۴۱-۵۰: AI آفلاین (بیزین، تطبیقی، Coach V2، TTS، احساس، پیشنهاد، گزارش، چت فندقی، آفلاین ۱۰۰٪، ضد اعتیاد)
- ✅ فاز ۵۱-۶۰: گیمیفیکیشن (بالانس، مأموریت، تقویم شمسی، ۲۴ مدال، چرخ شانس، آلبوم، ۵۰ مرحله)
- ✅ فاز ۶۱-۷۰: والدین و امنیت (قفل PIN، 20-20-20، بدون اینترنت، RAM، باتری، autosave، محافظت خرید، شب مهربان)
- ✅ فاز ۷۱-۸۰: استور و قانون (بریج پرداخت، flavor، اشتراک، اسکرین‌شات، سئو، ریتینگ، COPPA، لایسنس، چندزبانه، چک‌لیست)
- ✅ فاز ۸۱-۹۰: QA (Unit/Widget/Integration تست، مستندات گوشی ضعیف/استرس/حجم/CI/والدین/گزارش)
- ✅ فاز ۹۱-۱۰۰: انتشار (نسخه ۵، Alpha، کمپین، بازخورد، آنالیتیکس، نقشه ۲۰۲۷، متن‌باز، جشن، کتابچه PDF، تگ)

**نکته مهم:** بیلد APK/AAB و اجرای واقعی تست‌ها باید در CI (فاز ۸۸ — نیازمند مجوز workflows توسط مالک) یا روی دستگاه انجام شود؛ سندباکس Arena امکان اجرای Flutter را نداشت.

---
## فاز ۷۱-۸۰: کسب‌وکار، استور، قانون

- **فاز ۷۱:** بریج نیتیو پرداخت `MainActivity.kt` (MethodChannel kudake_iran/billing) — sandbox در debug، failure امن در release
- **فاز ۷۲:** راهنمای flavor دو استور `docs/PHASE_72_STORES.md`
- **فاز ۷۳:** اقتصاد اشتراک + A/B تست `docs/PHASE_73_SUBSCRIPTION.md`
- **فاز ۷۴:** لیست ۸ اسکرین‌شات استور `docs/PHASE_74_STORE_ASSETS.md`
- **فاز ۷۵:** توضیحات ۴۰۰ کلمه‌ای سئو کافه‌بازار `docs/PHASE_75_STORE_DESCRIPTION.md`
- **فاز ۷۶:** دیالوگ امتیاز در صفحه درباره ما (آفلاین، بدون درخواست مزاحم)
- **فاز ۷۷:** مستند COPPA `docs/PHASE_77_COPPA.md`
- **فاز ۷۸:** ممیزی لایسنس — همه ۱۷ asset موجود و ارجاع‌ها سالم
- **فاز ۷۹:** لایه رشته‌های چندزبانه پنهان `lib/core/app_strings.dart`
- **فاز ۸۰:** چک‌لیست ۳۰ موردی انتشار `docs/PHASE_80_RELEASE_CHECKLIST.md`

## فاز ۸۱-۸۳: تست‌ها

- **فاز ۸۱:** Unit Test — `achievement_system_test` (۲۴ مدال)، `ai_system_test` (تطبیقی/پیشنهاد/چت)، `game_data_test` (جزیره/مأموریت/فونت/استرس ۵۰۰)، `monetization_test`
- **فاز ۸۲:** Widget Test — `child_touch_target_test` (ناحیه ۶۴px) + به‌روزرسانی `app_smoke_test` برای Riverpod
- **فاز ۸۳:** Integration Test — `integration_test/app_flow_test.dart` + پکیج integration_test

---
## فاز ۴۱-۵۰: هوش مصنوعی آفلاین

- **فاز ۴۱:** موتور بیزین (Beta) برای تخمین تسلط مهارت‌ها (`AI.masteryOf`)
- **فاز ۴۲:** سختی تطبیقی — ۳ درست→سخت‌تر، ۲ غلط→آسان‌تر (`AI.adaptiveLevel`)
- **فاز ۴۳:** فندقی Coach V2 — یادآوری نام کودک، پیام‌های همدلی و جشن
- **فاز ۴۴:** `AudioService.speakNumber` (تلفظ فارسی اعداد) + TTS از قبل
- **فاز ۴۵:** تشخیص احساس — غلط‌های متوالی → «بریم بازی آسون‌تر» به‌جای سرزنش (در کوییز)
- **فاز ۴۶:** پیشنهاد ۳ بازی روزانه بر اساس مهارت ضعیف در داشبورد
- **فاز ۴۷:** کارت «پیشنهاد هوشمند فندقی» در پنل والدین + پیش‌بینی یک ماه
- **فاز ۴۸:** دوست خیالی فندقی — چت آفلاین قانون‌محور (`BuddyChatScreen`)
- **فاز ۴۹:** ابزار ممیزی حریم خصوصی `tools/privacy_audit.sh` — همه ۵ چک پاس
- **فاز ۵۰:** ضد اعتیاد — بعد از ۲۰ دقیقه، کارت «استراحت کوچک» در داشبورد

---
## فاز ۲۱-۳۰: آکادمی محتوایی + داستان + کوییز هوشمند

- **فاز ۲۱:** مرحله «بگو» در آکادمی الفبا (تلفظ TTS بعد از نوشتن) + ۳۴ حرف
- **فاز ۲۲-۲۸:** مرجع محتوای مرکزی `learning_topics.dart` (اعداد ۱-۲۰، ۱۲ رنگ، ۱۰ شکل، ۳۰ حیوان ایران، میوه/بدن، ۲۰ شغل + ۸ احساس، ۱۶ مفهوم) + موتور بازی مشترک `AcademyGame` (بشنو و پیدا کن / این چیه؟) + آزمایشگاه رنگ (`ColorsLabGame`)
- **فاز ۲۹:** ۱۰ داستان تعاملی شاخه‌ای با راوی فندقی (`StoryScreen`)
- **فاز ۳۰:** کوییز هوشمند — تولید سوال از روی ضعیف‌ترین مهارت (AI.weakSkill)

## فاز ۳۱-۴۰: موتور بازی‌ها

- **فاز ۳۱:** Memory Pro — ۵ سطح سختی + ۳ تم ایرانی (کلاسیک/حیوانات/میوه‌ها)
- **فاز ۳۲:** Bubble Pop — ترکیدن زنجیره‌ای + تلفظ هدف (حرف/عدد/رنگ)
- **فاز ۳۳/۳۴:** Star Catch (قلب ۵تایی) و Drawing (قلم/پاک‌کن/استیکر/Undo-Redo) از قبل کامل بودند — بررسی و تأیید شد
- **فاز ۳۵:** پازل کشیدنی ۲×۲/۳×۳ (`PuzzleGame`)
- **فاز ۳۶:** مسابقه ماشین ریاضی (`MathRaceGame`)
- **فاز ۳۷:** الگویاب (`PatternGame`)
- **فاز ۳۸:** بشنو و پیدا کن (`SoundMatchGame`)
- **فاز ۳۹:** بازی بدن و حواس با کشیدن آهنربایی (`BodyPartsGame`)
- **فاز ۴۰:** جزیره‌سازی (Meta Game) با ذخیره دائمی تزئینات (`IslandBuilderGame` + GameData.islandDecorations)

---
## فاز ۱۹: سیستم راهنمای هوشمند فندقی

- `armSmartHint`/`noteActivity`/`cancelSmartHint` در FandoghiCoach: راهنما فقط بعد از ۳ ثانیه بی‌حرکتی کودک
- اتصال به کوییز: لغو راهنما هنگام پاسخ، راهنمای سوال اول/بعدی

## فاز ۲۰: کیت تست کاربری ۱۰ کودک

- `docs/PHASE_20_USABILITY_TEST.md`: پروتکل ۲۰ دقیقه‌ای + برگه مشاهده + معیار موفقیت
- اجرای واقعی نیازمند والدین/تیم روی دستگاه است (مرحله خارج از کد)

---
## فاز ۱۰: مستندسازی زنده

- به‌روزرسانی `ARCHITECTURE.md`: معماری لایه‌ای (UseCase/Provider/DataSource)، دیاگرام mermaid جریان داده، ذخیره‌سازی Hive و تم داینامیک
- ایجاد مرکز مستندات `docs/` با ایندکس و گزارش فازها

---
## فاز ۹: اسکریپت Build حرفه‌ای

- بازنویسی کامل `build.sh` با چک ۷ مرحله‌ای شفاف: محیط → پروژه → pub get → آیکون → analyze → تست+پوشش → بیلد+حجم
- توقف واضح با پیام خطای فارسی به‌جای خاموش‌ماندن (رفع «قاتل خاموش»)
- نمایش درصد پوشش از coverage/lcov.info
- چک وجود آیکون در mipmap بعد از flutter_launcher_icons

---
## فاز ۸: لاگ و Crash Report آفلاین

- `CrashReportStore` جدید (Hive): ذخیره ۵۰ خطای آخر با زمان/منبع/stack به‌صورت کاملاً محلی
- `LoggerService`: خطاهای runtime خودکار persist می‌شوند
- هوک‌های سراسری `FlutterError.onError` و `PlatformDispatcher.onError` در main.dart
- کارت «گزارش خطاهای دستگاه» در پنل والدین: مشاهده/پاک کردن خطاها بدون نیاز به اینترنت

---
## فاز ۷: سیستم تم و فونت داینامیک

- `ThemeController` جدید: تم چرخه روز (صبح/ظهر/شب) + حالت سیستم + مقیاس فونت والدین، به‌روزرسانی هر ۱۵ دقیقه و هنگام برگشت از پس‌زمینه
- اتصال تم داینامیک به `MaterialApp` (جایگزینی theme/darkTheme ثابت)
- تنظیم «اندازه متن» در پنل والدین (۰٫۸۵ تا ۱٫۴ برابر) ذخیره‌شده در GameData + Hive
- فونت Baloo Bhaijaan 2 / Vazirmatn با fallback امن حفظ شد

---
## فاز ۶: طراحی سیستم صوتی

- بازنویسی `AudioService`: صف TTS (بدون overlap)، افکت‌های آماده تشویق/درست/غلط/برد (playCorrect, playWrong, playCoin, playWin)
- تلفظ فارسی حروف الفبا (`pronounceLetter`) متصل به آکادمی الفبا
- تشویق صوتی فندقی در کوییز هم‌زمان با حباب گفتگو
- مدیریت خطای کامل + لاگ به‌جای print

---
## فاز ۵: سیستم Asset Manager

- بازنویسی `AssetManager`: کش LRU با بودجه ۲۴ ورودی، لود تنبل، `filterQuality` قابل تنظیم برای گوشی ۱GB RAM
- `FandoghiImage` با fade-in، errorBuilder و کیفیت قابل تنظیم
- اتصال کش به `IllustrationTile` (تصاویر پرکاربرد بازی‌ها) و `PremiumLoading`
- تصمیم مهندسی مستند: AVIF در موتور Flutter 3.24 پشتیبانی نمی‌شود؛ بهینه‌سازی واقعی با lazy+cache+filterQuality

---
## فاز ۴: دیتابیس حرفه‌ای — Hive + Migration خودکار

- ساخت `HivePlayerStore` با اسکیمای نسخه‌دار (`schema_version`) در `lib/data/datasources/hive_player_store.dart`
- GameData: خواندن از Hive اول، Fallback به SharedPreferences قدیمی + مهاجرت خودکار در اولین save
- `monetization.dart`: حذف SharedPreferences مستقیم → Hive (توکن اشتراک/تاریخ خرید)
- سازگاری کامل با نسخه‌های قبلی حفظ شد (هر دو منبع هم‌زمان نوشته می‌شوند)

---
## فاز ۳: سیستم مدیریت State — مهاجرت به Riverpod

- ساخت `gameStateProvider` (StateNotifier + Snapshot غیرقابل‌تغییر) در `lib/presentation/providers/game_state_provider.dart`
- مهاجرت ۶ صفحه اصلی (Home, Dashboard, Profile, Shop, Parent, StageMap) از `addListener` دستی به `ref.watch(gameStateProvider)`
- کانال‌های گذرا (Coach overlay, موقعیت فندقی) به‌عنوان ChangeNotifier حفظ شدند (طبق الگوی Riverpod + ChangeNotifier)

---
## فاز ۲: معماری Clean — لایه UseCase

- ساخت `lib/domain/usecases/`: `GetPlayerProfile`، `RecordAnswer`، `CompleteStage`، `UpdateSettings`
- تزریق UseCaseها به اپ از طریق `lib/presentation/providers/use_case_providers.dart` (Riverpod)
- اتصال موجودیت `GameStage` به نقشه مرحله‌ای (منبع حقیقت پیشرفت مرحله)

---
# 🚀 کودک ایران — گزارش اجرای فازها (ROADMAP 100 فاز)

## فاز ۱: Audit کامل کد — ۹ اوت ۲۰۲۶

- تحلیل کامل `lib/` (۹۰ فایل) و تولید لیست بدهی فنی ۱۳ موردی در `docs/PHASE_1_AUDIT.md`
- حذف کد مرده: `animate_ext.dart`، `premium_router.dart`، `achievements_screen.dart` (فایل تکراری)
- اتصال `LoggerService` (talker) به `AudioService` و `main.dart` — حذف `print()`
- راه‌اندازی مستند اجرای فازها: `docs/PHASE_EXECUTION_LOG.md`

---
# 🚀 کودک ایران — نسخه ۴.۵.۰+۵
## فاز چهارم: Premium Navigation + Onboarding + Micro Interactions — ۸ اوت ۲۰۲۶

### ✨ فاز چهارم
- ویجت `PremiumButton` با انیمیشن فشاری و haptic feedback
- ارتقای کامل صفحه Onboarding با انیمیشن‌های staggered و دکمه‌های Premium
- آماده‌سازی `PremiumPageRoute` برای ناوبری نرم در کل اپ
- Micro-interaction در تمام دکمه‌ها و کارت‌ها

### ✨ فاز دوم: داشبورد Premium
- کارت‌های Premium Glass با سایه و انیمیشن فشاری
- جایگزینی کامل کارت‌های آمار با `PremiumCard`
- بهبود انیمیشن‌های staggered در بخش‌های ماموریت و دسته‌بندی‌ها
- آماده‌سازی `PremiumPageRoute` برای transitionهای نرم بین صفحات

### ✨ ویژگی‌های جدید و حرفه‌ای
- **پاورآپ‌های هوشمند** در بازی ستاره‌گیری: قلب جادویی ❤️، الماس امتیاز دوبل 💎
- **آیتم‌های آموزشی جدید**: سیب قرمز 🍎، گل بهاری 🌸، پروانه 🦋 (برای تقویت شناخت طبیعت)
- **سیستم هدف پویا** با ۸ نوع آیتم آموزشی مختلف
- **بهبود بازخورد فندقی** با پیام‌های تشویقی و آموزشی غنی‌تر
- **افزایش جان حداکثری** به ۵ قلب (حمایت بیشتر از کودکان)
- **انیمیشن و هپتیک** بهبود یافته برای تجربه لذت‌بخش‌تر

### 🛠️ بهبودهای فنی (مطابق AI_PROJECT_RULES)
- رعایت کامل Clean Code و DRY
- بهینه‌سازی عملکرد بازی (کاهش مصرف CPU)
- حفظ کامل حالت آفلاین و امنیت داده
- آماده‌سازی برای تست و بیلد release

---

# 🚀 کودک ایران — نسخه ۴.۱.۲+۴
## اصلاحات فندقی، تجربه کاربری و QA — ۸ اوت ۲۰۲۶

- جایگزینی تصویر فندقی با کاراکتر مربی شفاف و بدون پس‌زمینه یا هاله سفید.
- اصلاح نمایش تصویر در Splash، onboarding، داشبورد و حباب‌های مربی تا تصویر و متن هماهنگ باشند.
- اتصال حباب گفت‌وگوی فندقی به موقعیت خود کاراکتر؛ متن هنگام جابه‌جایی همراه فندقی حرکت می‌کند.
- حفظ نقطه لمس هنگام Drag و جلوگیری از خروج کامل کاراکتر از لبه‌های صفحه.
- اصلاح چند باگ بازی‌ها: ریست هدف و کمبو هنگام شروع مجدد، حفظ درجه سختی بازی حافظه و فعال‌سازی درست محدودیت زمان.
- اصلاح rollover مأموریت‌ها و زمان بازی هنگام باز ماندن برنامه در نیمه‌شب.
- افزایش `versionName` به `4.1.2` و `versionCode` به `4`.

---

# 🚀 کودک ایران — نسخه ۴.۱.۱+۳
## اصلاح خروجی release: آیکون لانچر در APK/AAB — ۷ اوت ۲۰۲۶

- شماره نسخه به `4.1.1+3` رسید تا بیلد جدید release از `4.1.0+2` متمایز شود.
- **مشکل:** پایپ‌لاین GitHub Actions فایل‌های آیکون را قبل از build تولید نمی‌کرد
  و APK با آیکون پیش‌فرض Flutter ساخته می‌شد.
- **راه‌حل:** مرحله‌ی `dart run flutter_launcher_icons` باید بعد از `flutter pub get`
  و قبل از `flutter build` اجرا شود (در `codemagic.yaml` از قبل وجود دارد؛ در
  `build.sh` هم هست). diff آماده‌ی اعمال در `.github/workflows/build-apk.yml`
  در بدنه‌ی PR آمده است؛ تغییر فایل‌های workflow به مجوز `workflows` نیاز دارد
  و باید توسط مالک مخزن اعمال شود.
- نام artifactهای CI به `amoozesh_fandoghi_v4.1.0_apk/aab` به‌روزرسانی می‌شود.

---

# 🚀 کودک ایران — نسخه ۴.۱.۰+2
## گزارش QA سخت‌گیرانه — ۷ اوت ۲۰۲۶

### سازنده و پشتیبانی
- **سازنده/ناشر:** فرشاد پارسا
- **ایمیل:** farshadparsa2019@gmail.com
- **تلگرام:** @Parsaappsadmin

### اصلاحات مهم
- اضافه شدن onboarding امن و محلی برای لقب/سن تقریبی کودک.
- ارتقای فندقی به مربی و داور مرکزی با حباب‌های پاپ‌آپ سراسری و بازخورد درست/غلط در همه بازی‌ها.
- ساخت آکادمی الفبا با آموزش دیداری، تمرین نوشتن و ارزیابی محلی مسیر کودک.
- جایگزینی شکلک‌های حافظه و آزمون با sprite sheetهای کارتونی WebP اختصاصی برای موضوعات مختلف.
- ارتقای نقاشی با پاک‌کن، ابزار قلم، Undo/Redo و استیکرهای تصویری.
- اصلاح routing: موضوعات آموزشی به آزمون واقعی و نقاشی به canvas واقعی می‌روند.
- اصلاح تایمر حافظه، جلوگیری از پاداش تکراری و اتصال موفقیت مرحله به نقشه.
- اضافه شدن ثبت پاسخ درست/غلط، مهارت‌ها، مأموریت‌ها و محدودیت زمان روزانه.
- مقاوم‌سازی ذخیره‌سازی SharedPreferences در برابر write race و داده خراب.
- جلوگیری از فعال‌سازی AI از طریق toggle جعلی پنل والدین.
- سخت‌گیری رسید پرداخت در release و consume خریدهای مصرف‌شدنی قبل از اعطای موجودی.
- اجرای analyze/test/build در CI و حذف وابستگی‌های بلااستفاده.

### وضعیت انتشار
- این commit بدون اجرای واقعی Flutter در محیط حاضر قابل اعلام به‌عنوان APK نهایی نیست؛
  Flutter در sandbox موجود نیست و CI باید مراحل release را اجرا کند.
- اتصال Native SDK کافه‌بازار/مایکت و تست با حساب sandbox هنوز پیش‌نیاز انتشار است.
- قبل از ارسال، تست دستی روی دستگاه ضعیف، RTL، حالت شب، قطع اینترنت، lifecycle و
  خرید ناموفق الزامی است.

---

# 🚀 کودک دانا - نسخه ۳.۱.۱
## آماده انتشار در کافه‌بازار و مایکت

---

## 🆕 تغییرات نسخه ۳.۱.۱
- **آیکون جدید اپلیکیشن:** جایگزینی آیکون با طرح کاراکتر کودکانه (دختر کارتونی
  با ستاره زرد و ابر خندان) با رنگ‌های گرم
- **آیکون تطبیقی (Adaptive Icon):** افزودن فوره‌گراند اختصاصی با کارت سفید
  گرد روی پس‌زمینه زرد — نمایش تمیز در همه لانچرها و ماسک‌های اندروید
- **اصلاح پایپ‌لاین بیلد:** افزودن مرحله `flutter_launcher_icons` به ورک‌فلوهای
  GitHub Actions و Codemagic تا آیکون جدید واقعاً در APK/AAB اعمال شود
- **اصلاح کانفیگ:** `android: true` برای تولید خودکار `ic_launcher` در همه
  mipmap ها (قبلاً نام آیکون با مانیفست همخوانی نداشت و آیکون سفارشی در
  خروجی اعمال نمی‌شد)
- ارتقای نسخه به `3.1.1+8`

---

# 🚀 کودک دانا - نسخه ۳.۱.۰
## آماده انتشار در کافه‌بازار و مایکت

---

## 🐛 اصلاحات
- **فروشگاه:** رفع باگ «دوست AI» — حالا وضعیت خریداری‌شده به‌صورت پایدار
  ذخیره می‌شود و آیتم بعد از خرید دوباره قابل خرید نیست (قبل از این، ۱۰۰ ستاره
  به‌صورت بی‌پایان کسر می‌شد).
- **فروشگاه:** جلوگیری از خرید دوباره استیکرها (کسر سکه تکراری و id تکراری).
- **فروشگاه:** نمایش قیمت «دوست AI» با «۱۰۰ ستاره» به‌جای «۴۵۰ سکه» برای
  هماهنگی با منطق خرید.
- **جزیره یادگیری:** قفل ماژول AI حالا به خرید اشتراک/AI وصل است و با خرید
  واقعاً باز می‌شود.
- **بازی ترتیب اعداد:** رفع باگ تشخیص ترتیب — انتخاب عددِ خارج از ترتیب حالا
  بلافاصله بازخورد می‌گیرد و شمارش از اول شروع می‌شود.
- **جایزه فندقی:** دیالوگ جایزه دیگر با کلیک بیرون رد نمی‌شود تا جایزه از
  دست نرود.

---

## 🆕 تغییرات نسخه ۳.۱.۰
- **آیکون جدید اپلیکیشن:** جایگزینی آیکون اصلی با طرح اختصاصی «کودک ایران ۱ ۲ ۳»
- پل پرداخت کافه‌بازار/مایکت (`BillingService`) با MethodChannel اختصاصی
- فعال‌سازی اشتراک فقط بعد از تایید پرداخت توسط استور
- حالت سندباکس برای تست خریدها در بیلد debug

---

## ✨ ویژگی‌های کلیدی نسخه ۳.۰

### 🎨 طراحی حرفه‌ای (مطابق با تصاویر مرجع)
- **جزیره شناور با حیوانات** (گربه، میمون، خرگوش، پاندا، لاک‌پشت، پروانه)
- **نقشه مرحله‌های جادویی** با مسیر winding و انیمیشن
- **Toggle Magic Island / Free Play**
- **هدر حرفه‌ای** با آمار زنده (ستاره، streak، لول)
- **طراحی کاملاً کودک‌پسند** و رنگارنگ

### 💰 سیستم کسب درآمد
- **اشتراک ویژه (کودک دانا پلاس)**
  - ماهانه: ۴۹,۰۰۰ تومان
  - سالانه: ۳۹۹,۰۰۰ تومان (۳۰٪ تخفیف)
- **فروشگاه داخل برنامه**
  - خرید سکه و ستاره
  - خرید استیکر و آیتم‌های ویژه
  - AI Buddy (دوست هوش مصنوعی)
- **جایزه‌های روزانه و هفتگی**
- **صندوق گنج** (با تکمیل ماموریت‌ها)

### 🎮 محتوای آموزشی
- ۲۵+ بازی آموزشی تعاملی
- سیستم AI هوشمند (تنظیم سختی بر اساس عملکرد)
- ۸ مرحله نقشه جادویی
- ۸ جزیره یادگیری
- ماموریت‌های روزانه
- مدال‌ها و دستاوردها

### 👨‍👩‍👧 پنل والدین
- آمار کامل پیشرفت
- گزارش مهارت‌ها (نمودار)
- محدودیت زمانی
- کنترل صدا و تنظیمات

---

## 📱 صفحات اصلی

| صفحه | توضیحات |
|------|---------|
| **خانه** | داشبورد + ماموریت‌ها + بازی‌های سریع |
| **جزیره** | جزیره شناور با حیوانات (طراحی تبلت) |
| **نقشه** | نقشه جادویی با ۸ مرحله |
| **پروفایل** | آمار + مهارت‌ها + مدال‌ها |
| **جایزه** | صندوق جایزه + گنج |
| **فروشگاه** | خرید سکه، ستاره، استیکر |
| **اشتراک** | پلن‌های ماهانه و سالانه |

---

## 🛠️ آماده‌سازی برای انتشار

### ۱. اتصال به کافه‌بازار و مایکت
لایه پرداخت در `lib/core/billing_service.dart` پیاده‌سازی شده است.
این لایه از طریق MethodChannel با نام `kudake_iran/billing` به ماژول نیتیو
اندروید وصل می‌شود. مراحل نهایی:

- ماژول نیتیو اندروید را پیاده‌سازی کنید (سه متود `purchase`، `consume` و `restore`)
  طبق مستندات داخل `billing_service.dart` — با Billing کافه‌بازار یا SDK مایکت.
- شناسه محصولات (`sub_monthly` و `sub_yearly`) را در پنل کافه‌بازار/مایکت بسازید.
- قبل از انتشار، ماژول Native پرداخت را متصل کنید؛ fallback سندباکس در release
  به‌صورت compile-time خاموش است.

> تا قبل از اتصال ماژول نیتیو، خریدها در بیلد debug به‌صورت آزمایشی
> (سندباکس) موفق می‌شوند تا فلوهای اپ قابل تست باشند؛ اما در بیلد release
> بدون ماژول نیتیو هیچ خریدی فعال نمی‌شود.

### ۳. آیکون و نام
- آیکون در `assets/icon.png` موجود است
- نام اپ: **کودک دانا** / **Smart Kid**

### ۴. تست نهایی
```bash
flutter analyze
flutter test
flutter build appbundle --release
```

---

## 📊 آمار آماده انتشار

- **نسخه**: 3.1.1+8
- **SDK**: Flutter 3.24+
- **حداقل Android**: 21
- **تعداد صفحات**: ۸ صفحه اصلی
- **تعداد بازی**: ۲۵+
- **ویجت‌های سفارشی**: ۴ ویجت حرفه‌ای

---

**توسعه‌دهنده**: Arena.ai Agent + تیم پارسا اپس
**تاریخ**: ۳۱ تیر ۱۴۰۵

**یادداشت تاریخی:** ادعای آماده‌بودن نسخه‌های قدیمی جایگزین چک‌لیست QA نسخه ۴.۱ نیست؛
قبل از انتشار باید pipeline، SDK پرداخت و تست دستگاه واقعی با موفقیت انجام شوند.