# 🔐 گزارش ممیزی امنیتی و سخت‌سازی «جزیره فندقی» — نسخه ۶.۳

> **تاریخ:** ۲۰۲۶-۰۸-۱۲
> **دامنه:** اپلیکیشن اندروید Flutter (جزیره فندقی / کد `jazireh_fandoghi`)، کافه‌بازار، کاملاً آفلاین به‌جز کارتون‌ها
> **نوع کار:** ممیزی امنیتی کامل (static analysis) + شبیه‌سازی حملات + پیاده‌سازی ضد-هک

---

## خلاصه مدیریتی (Executive Summary)

اپ در وضعیت امنیتی **قابل قبول تا خوب** قرار داشت: `allowBackup` خاموش، R8/ProGuard فعال،
پرداخت کافه‌بازار با تأیید امضای RSA (Poolakey)، بدون ترافیک HTTP صریح، بدون فعالیت‌های
export شده‌ی غیرضروری و بدون جمع‌آوری دادهٔ کودک. با این حال **۵ آسیب‌پذیری واقعی و ۳
نقطه‌ضعف** شناسایی شد که جدی‌ترین آن‌ها مسیرهای دور زدن نسخهٔ پریمیوم و حذف قفل والدین از
طریق دستکاری فایل‌های ذخیره‌سازی بود.

در این نسخه (۶.۳) همهٔ آسیب‌پذیری‌ها برطرف و ضد-هک‌های لایه‌ای زیر پیاده شد:

| لایه | کنترل |
|---|---|
| صحت بیلد | بررسی امضای APK در زمان اجرا + بلاک کامل بیلدهای دستکاری‌شده |
| ضد ابزار هک | شناسایی Frida / Xposed / دیباگر / روت / ایمولاتور |
| درآمد | گرنت پریمیوم در Android Keystore (جعل با ویرایش فایل غیرممکن شد) |
| قفل والدین | هش PIN با PBKDF2 (۱۵۰٬۰۰۰ دور) + کپی مرجع در Keystore |
| بکاپ | رمزنگاری v4 با کلید مشتق‌شدهٔ PBKDF2 + salt (شکستن دیکشنری غیرممکن شد) |
| حریم خصوصی والد | FLAG_SECURE روی پنل والدین/پرداخت (بدون اسکرین‌شات و ضبط) |

---

## ۱) روش‌شناسی (چطور «هکش کردم»)

چون هیچ سروری وجود ندارد (اپ ۱۰۰٪ آفلاین است)، حمله به سه دسته تقسیم می‌شود:

1. **هک APK** — دانلود APK، decompile با `jadx`/`apktool`، تغییر `smali`/منابع، بسته‌بندی مجدد و امضای دوباره (repackage) برای توزیع نسخهٔ آلوده با نام اپ.
2. **هک زمان اجرا** — اتصال Frida/Objection برای هوک کردن متدها و جعل پاسخ‌ها (مثلاً جعل موفقیت خرید).
3. **هک دادهٔ محلی** — دستکاری فایل‌های Hive/SharedPreferences روی دستگاه root شده برای: فعال‌کردن پریمیوم، حذف PIN والدین، دور زدن ساعت خواب/محدودیت‌ها.

هر یافته با مسیر حملهٔ واقعی، شدت (Critical/High/Medium/Low)، و وضعیت رفع مشخص شده است.

---

## ۲) سطح حمله (Attack Surface)

| سطح | اقلام |
|---|---|
| ذخیره‌سازی محلی | Hive (`playerBox`، `crash_logs`)، SharedPreferences، بکاپ `.parsa`، گرنت پریمیوم |
| شبکه | فقط HTTPS به `www.aparat.com` و `api.aparat.com` (کارتون و پوستر) |
| پرداخت | MethodChannel `kudake_iran/billing` → Poolakey (کافه‌بازار) |
| اجزای اندروید | فقط `MainActivity` (exported، لازم برای launcher)؛ انتخاب عکس با Photo Picker سیستم و بدون Activity یا مجوز ذخیره‌سازی اضافه |
| سطح‌های دیگر | هیچ WebView، هیچ deep link، هیچ سرویس/گیرندهٔ broadcast، هیچ ContentProvider |

---

## ۳) یافته‌ها — حملاتی که انجام و اثبات شد

### 🔴 CRITICAL-1 — بسته‌بندی مجدد و توزیع نسخهٔ آلوده (Repackaging)

**حمله:** APK رسمی را با `apktool` باز کنید، `lib/` و منابع را با ad یا بدافزار تزریق کنید،
دوباره با کلید دلخواه امضا کنید و در سایت‌های APK فارسی پخش کنید. چون هیچ بررسی امضایی در
زمان اجرا وجود نداشت، نسخهٔ آلوده مثل نسخهٔ اصلی اجرا می‌شد — **خطر واقعی برای یک اپ کودک:**
توزیع بدافزار/تبلیغات مزاحم با نام تجاری اپ.

**وضعیت: ✅ رفع شد**
- هنگام ساخت release، SHA-256 گواهی امضای keystore به‌طور خودکار در `BuildConfig.EXPECTED_SIGNING_SHA256` ثبت می‌شود (فایل `android/app/build.gradle`).
- در اجرای release، ماژول نیتیو امضای APK نصب‌شده را با مقدار ثبت‌شده مقایسه می‌کند (`SecurityModule.signingCertificateSha256`).
- اگر امضا متفاوت باشد (یا ماژول امنیتی از بیلد حذف شده باشد) → **صفحهٔ تمام‌صفحهٔ «این نسخه قابل تأیید نیست»** نمایش داده می‌شود و اپ اجرا نمی‌شود (`TamperBlockScreen`).
- نکته: منطق بررسی در Dart (داخل `libapp.so` کامپایل AOT) قرار دارد که پچ کردن آن با ابزارهای smali عملاً ممکن نیست؛ تغییر سمت Kotlin هم امضا را می‌شکند و توسط همین بررسی گرفته می‌شود.

### 🔴 CRITICAL-2 — جعل پریمیوم با دستکاری فایل Hive

**حمله:** روی دستگاه root، فایل `playerBox.hive` را باز کنید و مقدار `has_full_version = true` بگذارید.
نسخهٔ قبل کلید «خرید کامل» را در Hive ساده نگه می‌داشت و فقط یک بازیابی استور انجام می‌داد؛
بدتر از آن، **در حالت آفلاین** (که این اپ برای آن طراحی شده) بازیابی شکست می‌خورد و حتی
خریدار واقعی هم قفل می‌شد — بنابراین مسیر «گرنت محلی» عملاً یک نقطهٔ جعل بود.

**وضعیت: ✅ رفع شد**
- گرنت پریمیوم + توکن رسید حالا در **Android Keystore** (AES-256/GCM) نگهداری می‌شود؛ ویرایش Hive/prefs دیگر اثری ندارد (`SecureStore` نیتیو + `Monetization`).
- فلگ متنی قدیمی در release دیگر **هرگز مستقیم قبول نمی‌شود**: فقط برای مهاجرت یک‌بارهٔ کاربران قانونیِ قبل از ۶.۳، و آن هم **فقط وقتی** توکن رسیدِ معتبر-شکل همراه باشد و استور در دسترس نباشد. فلگ بدون توکن جعلی تشخیص داده و پاک می‌شود.
- رفتار آفلاین هم درست شد: خریدار قانونی بدون اینترنت دیگر از بخش‌های پریمیوم قفل نمی‌شود (گرنت Keystore).

### 🔴 CRITICAL-3 — حذف قفل والدین با دستکاری حافظه (Parental-control bypass)

**حمله:** هش PIN والدین (`parentPinHash`) در SharedPreferences و اسنپ‌شات Hive بدون هیچ
محافظ یکپارچگی ذخیره می‌شد؛ کافی بود مقدار را خالی کنید تا پنل والدین بدون PIN باز شود و
ساعت خواب/فیلتر محتوا/محدودیت زمان خاموش شود.

**وضعیت: ✅ رفع شد**
- کپی **مرجع** هش PIN در Keystore نگهداری می‌شود؛ هنگام `load()` مقدار Keystore جایگزین مقدار دستکاری‌شدهٔ فایل‌ها می‌شود (`GameData._applySecurePinOverride`).
- حذف PIN از طریق UI، هر سه کپی (Keystore + prefs + Hive) را همزمان پاک می‌کند؛ دستکاری دستی فقط فایل‌ها را عوض می‌کند و در راه‌اندازی بعدی اصلاح می‌شود.

### 🟠 HIGH-4 — شکستن سریع PIN والدین (هش ضعیف)

**حمله:** هش PIN با SHA-256 خام بدون salt ذخیره می‌شد. PIN فقط ۴ رقم است (۱۰٬۰۰۰ ترکیب)؛
با یک GPU، کل فضای ترکیب‌ها در **چند ثانیه** قابل brute-force بود و هشِ داخل فایل مستقیماً
قابل کپی بود. همین ضعف در کلید بکاپ v3 هم بود (بند بعد).

**وضعیت: ✅ رفع شد**
- هش جدید: **PBKDF2-HMAC-SHA256 با ۱۵۰٬۰۰۰ دور و salt تصادفی ۱۶ بایتی** + مقایسهٔ constant-time (`GameData.hashParentPin`).
- فرمت: `pbkdf2:<iterations>:<saltB64>:<hashB64>`؛ هش‌های قدیمی همچنان پذیرفته و در اولین ورود موفق خودکار ارتقا می‌یابند.
- محدودیت صادقانه: PIN ۴ رقمی ذاتاً فضای کمی دارد؛ در برابر حملهٔ آفلاینِ با قدرت بالا (root + GPU) هیچ راه‌حل ۱۰۰٪ سمت‌کلاینت وجود ندارد، اما هزینه به ازای هر PIN از «کمی‌کمتر از یک‌میلی‌ثانیه» به «چند صد میلی‌ثانیه» و با rate-limit UI (۵ بار خطا → ۳۰ ثانیه قفل) رسید.

### 🟠 HIGH-5 — رمزگشایی فایل بکاپ `.parsa` (کلید ضعیف v3 و کلید ثابت v2)

**حمله:** بکاپ v3 کلیدش را با `SHA-256("kudake-backup-v3:" + pin)` می‌ساخت → همان brute-force
۱۰٬۰۰۰ تایی فوری. بکاپ v2 (سازگاری قدیمی) کلید ثابت داخل سورس داشت → هر کسی که APK را
داشت می‌توانست بکاپ‌های v2 هر کاربری را رمزگشایی کند (شامل اطلاعات کودک).

**وضعیت: ✅ رفع شد**
- فرمت جدید **v4**: کلید با PBKDF2-HMAC-SHA256 (۱۵۰٬۰۰۰ دور) و salt تصادفی ۱۶ بایتی ذخیره‌شده در هدر فایل ساخته می‌شود؛ دو بکاپ با PIN یکسان هم خروجی متفاوت دارند.
- خواندن v3 و v2 فقط برای سازگاری با بکاپ‌های قبلی باقی مانده؛ خروجی جدید همیشه v4 است.
- محدودیت اندازهٔ فایل (۲ مگابایت)، اعتبارسنجی salt و مدیریت خطا هم حفظ و تکمیل شد.

### 🟡 MEDIUM-6 — ثبت اسکرین‌شات/ضبط صفحه هنگام ورود PIN و پرداخت

**حمله:** اپ‌های مخربِ دارای مجوز ضبط صفحه (یا خود کودک) می‌توانستند ورود PIN والدین و
فرایند پرداخت را ضبط کنند.

**وضعیت: ✅ رفع شد**
- پنل والدین، دیالوگ‌های PIN و شیت پرداخت زیر **FLAG_SECURE** قرار گرفتند (`SecureWindowScope` + کانال `kudake_iran/privacy`)؛ اسکرین‌شات و ضبط این بخش‌ها خالی ثبت می‌شود و پیش‌نمایش Recent هم خالی است.
- بقیهٔ اپ (بازی‌ها، گواهی‌ها) دست‌نخورده ماند تا والد بتواند از پیشرفت کودک اسکرین‌شات بگیرد.

### 🟡 MEDIUM-7 — ابزارهای داینامیک (Frida/Xposed) و بیلد debug

**حمله:** با Frida می‌شد پاسخ MethodChannel پرداخت را هوک کرد و `success=true` جعل کرد؛
یا یک بیلد debug (با `android:debuggable`) به‌جای release منتشر کرد.

**وضعیت: ✅ رفع شد**
- ماژول نیتیو `SecurityModule` موارد زیر را در زمان اجرا بررسی می‌کند: پورت‌های Frida (۲۷۰۴۲/۲۷۰۴۳)، کتابخانه‌های تزریق‌شده در `/proc/self/maps`، فایل‌های frida در `/data/local/tmp`، فایل‌های Xposed، اتصال دیباگر (`Debug.isDebuggerConnected`)، `debuggable` بودن release، روت (su/Magisk)، ایمولاتور.
- سیاست (policy): release ای که `debuggable` باشد یا دیباگر متصل باشد یا امضا نخواند → **بلاک**. Frida/Xposed/روت → هشدار + لاگ (کاربر عادی هرگز بلاک نمی‌شود؛ روت در بین خانواده‌های ایرانی رایج است).

### 🟢 LOW-8 — عدم TLS pinning برای آپارات

**حمله:** ترافیک کارتون HTTPS است، اما با نصب گواهی‌کاربری (MITM پروکسی/ISP) می‌شد محتوای
پخش را دستکاری کرد.

**وضعیت: ⚠️ مستند + کاهش ریسک**
- `network_security_config.xml` اضافه شد: ترافیک صریح (cleartext) به‌صورت صریح ممنوع شد.
- pinning کامل روی CDN آپارات **عمداً** انجام نشد چون هاست‌های CDN آپارات می‌چرخند و pin کردن می‌تواند پخش کارتون را برای کاربران واقعی بشکند (ریسک دست‌رس‌پذیری > ریسک MITM برای این محتوا). اگر بعداً سرور اختصاصی داشتید، pin را روی API خودتان اضافه کنید.

### ✅ نکات مثبت موجود (قبل از این نسخه)

- `allowBackup=false` + قوانین exclude کامل برای backup و device-transfer.
- R8 minify + shrinkResources + ProGuard.
- Poolakey با `SecurityCheck.Enable` (تأیید امضای RSA پاسخ‌های بازار).
- `sandboxFallback` فقط در debug؛ در release خرید بدون رسید پذیرفته نمی‌شود.
- بدون WebView، بدون deep link، بدون HTTP صریح، بدون مجوزهای غیرضروری (storage با maxSdk محدود شده).
- بدون سرویس ابری/تحلیل؛ دادهٔ کودک هرگز از دستگاه خارج نمی‌شود (CrashReportStore فقط محلی، max ۵۰ ورود).

---

## ۴) تغییرات پیاده‌سازی‌شده (فایل‌به‌فایل)

### نیتیو (Kotlin/Gradle)
| فایل | تغییر |
|---|---|
| `android/app/src/main/kotlin/.../SecurityModule.kt` | **جدید** — بررسی امضا، root، Frida، Xposed، دیباگر، ایمولاتور |
| `android/app/src/main/kotlin/.../SecureStore.kt` | **جدید** — ذخیره‌سازی Keystore (AES-256/GCM؛ fallback مستند API<23) |
| `android/app/src/main/kotlin/.../MainActivity.kt` | کانال‌های `kudake_iran/security`، `kudake_iran/secure_store`، `kudake_iran/privacy` |
| `android/app/build.gradle` | `EXPECTED_SIGNING_SHA256` به‌صورت خودکار از `key.properties` |
| `android/app/src/main/AndroidManifest.xml` | `usesCleartextTraffic=false` + `networkSecurityConfig` |
| `android/app/src/main/res/xml/network_security_config.xml` | **جدید** |
| `tools/print_cert_sha256.sh` | **جدید** — راستی‌آزمایی هش امضا |

### Dart
| فایل | تغییر |
|---|---|
| `lib/core/security/security_hardening.dart` | **جدید** — ارزیابی امنیتی + سیاست بلاک |
| `lib/core/security/secure_store.dart` | **جدید** — پل Dart به SecureStore نیتیو |
| `lib/core/security/privacy_protection.dart` | **جدید** — FLAG_SECURE + `SecureWindowScope` |
| `lib/features/security/tamper_block_screen.dart` | **جدید** — صفحهٔ بلاک نسخهٔ دستکاری‌شده |
| `lib/main.dart` | بررسی امنیتی در start-up + مسیر `/security-blocked` |
| `lib/core/game_data.dart` | PBKDF2 PIN + مرجع Keystore + override هنگام load |
| `lib/core/monetization.dart` | گرنت Keystore + مهاجرت امن + رفع قفل آفلاین |
| `lib/core/backup_service.dart` | بکاپ v4 با PBKDF2 + همگام‌سازی PIN |
| `lib/domain/usecases/update_settings.dart` | API پین ناهمگام |
| `lib/features/parent/parent_panel.dart` | await پین + FLAG_SECURE |
| `lib/features/shop/full_version_paywall.dart` | FLAG_SECURE روی پرداخت |
| `test/core/parent_pin_test.dart` | تست‌های فرمت جدید + سازگاری فرمت قدیمی |

---

## ۵) محدودیت‌ها — چه چیزهایی «هک نشدنی» نیست (صادقانه)

هیچ ضد-هکی روی دستگاه‌های root/Frida شده ۱۰۰٪ نیست؛ این‌ها اصولاً «بالا بردن هزینه» هستند:

1. **مهاجم root + Frida حرفه‌ای** می‌تواند فراخوانی‌های Dart را هوک کند یا کد را در حافظه پچ کند. این سطح از حمله برای همهٔ اپ‌های آفلاین بدون سرور اجتناب‌ناپذیر است — راه‌حل قطعی، **بک‌اند پرداخت/مجوز** است (مثلاً تأیید سرور-ساید رسید خرید با کلید خصوصی Bazaar).
2. **PIN ۴ رقمی** ذاتاً فضای کوچکی دارد؛ Keystore + PBKDF2 + rate-limit فقط هزینه را بالا می‌برد.
3. **API<23** (حدود ۰/۵٪ دستگاه‌ها در ۲۰۲۶) Keystore AES ندارند؛ SecureStore برای آن‌ها fallback ضعیف‌تر دارد (مستند در کد).
4. بررسی Frida/روت **best-effort** است و عمداً سخت‌گیرانه نیست تا کاربر عادی بلاک نشود؛ سیاست بلاک فقط برای سیگنال‌های قطعی (امضا، debuggable، دیباگر، حذف ماژول) است.

---

## ۶) چک‌لیست انتشار (قبل از آپلود در کافه‌بازار)

- [ ] `android/key.properties` با keystore واقعی وجود دارد (بدون آن release ساخته نمی‌شود).
- [ ] `android/billing.properties` با `bazaarRsaPublicKey` واقعی داشبورد بازار تنظیم است.
- [ ] ساخت `flutter build apk --release` موفق است؛ `EXPECTED_SIGNING_SHA256` غیرخالی است.
- [ ] راستی‌آزمایی امضا:
  ```bash
  tools/print_cert_sha256.sh android/release.keystore <alias> <pass>
  keytool -printcert -jarfile build/app/outputs/flutter-apk/app-release.apk | grep "SHA256:"
  ```
- [ ] تست روی یک گوشی واقعی: خرید/بازیابی پریمیوم، ورود PIN، خروجی/ورودی بکاپ، پخش کارتون.
- [ ] تست آفلاین: بعد از خرید، با خاموش‌کردن اینترنت بخش‌های پریمیوم باز می‌مانند.
- [ ] تست «دستکاری»: بیلد release را با کلید دیگر دوباره امضا کنید → اپ باید صفحهٔ بلاک را نشان دهد.
- [ ] `flutter test` و تست انتگراسیون سبز.

---

## ۷) پیشنهادهای آینده (در این نسخه انجام نشد)

| پیشنهاد | چرا انجام نشد |
|---|---|
| بک‌اند مجوز/پرداخت (کد گواهی سرور-ساید) | این اپ بدون سرور طراحی شده؛ اگر سرور اضافه شد، اولویت ۱ |
| TLS pinning روی API اختصاصی | تا وقتی CDN آپارات است، ریسک شکستن پخش > سود امنیتی |
| امضای فایل‌های asset (محتوای آموزشی) | محتوا غیرحساس/عمومی است؛ هزینه پیاده‌سازی بالا، سود پایین |
| Anti-debugging در لایهٔ C/C++ | پیچیدگی بالا برای سودی که Frida قبلاً گرفته |

---

## پیوست — معماری امنیتی جدید (یک‌نگاه)

```
اجرا (release)
  └─ main() → SecurityHardeningService.assess()
        ├─ signatureMatch ?→ بله؟ → ادامه / خیر؟ → TamperBlockScreen
        ├─ debuggable / دیباگر؟ → TamperBlockScreen
        ├─ Frida / Xposed؟ → لاگ + هشدار (ادامه)
        └─ root / emulator؟ → فقط اطلاع‌رسانی

پریمیوم
  └─ Monetization.hasFullVersion()
        ├─ (online) restore با تأیید RSA بازار → ذخیرهٔ گرنت در Keystore
        ├─ (offline) گرنت Keystore معتبر؟
        └─ مهاجرت فقط: فلگ قدیمی + توکن رسید معتبر-شکل + آفلاین

قفل والدین
  └─ GameData.verifyParentPin → PBKDF2(150k, salt) + مقایسهٔ constant-time
        └─ مرجع در Keystore؛ آینه در prefs/Hive؛ override هنگام load

بکاپ .parsa
  └─ v4 = AES-256-GCM با کلید PBKDF2(150k, salt تصادفی در هدر فایل)
